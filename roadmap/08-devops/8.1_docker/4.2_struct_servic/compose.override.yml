services:
  postgres:
    ports:
      - "127.0.0.1:5432:5432"

  redis:
    ports:
      - "127.0.0.1:6379:6379"

  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    environment:
      APP_ENV: development
      LOG_LEVEL: debug

  nginx:
    ports:
      - "${NGINX_PORT}:80"