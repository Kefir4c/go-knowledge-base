server {
    listen 80;

    location / {
        return 200 "hello from nginx\n";
        add_header Content-Type text/plain;
    }

    location /health {
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }
}