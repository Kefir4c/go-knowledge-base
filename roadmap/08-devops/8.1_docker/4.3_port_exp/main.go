package main

/*
  УРОК 4.3: ПОРТЫ И EXPOSE
  Кажется, что публикация порта — это мелочь: написал "8080:8080",
  и работает. На практике это одна из самых частых дыр в безопасности
  контейнерных деплоев. Postgres на 0.0.0.0:5432 в интернете — это
  приглашение для ботов, которые перебирают пароли. Redis без пароля
  на публичном порту — это утечка всех данных за час.
  Разница между ports и expose — это разница между «доступно
  всему интернету» и «доступно только внутри Compose-сети».
  Одно слово в yaml — и вектор атаки меняется кардинально.
  Эта тема про то, как правильно публиковать порты, где ставить
  127.0.0.1 вместо 0.0.0.0, и почему внутренние сервисы наружу
  торчать не должны.

  СОДЕРЖАНИЕ:
    1.  Проблема: почему порты — это дыра
    2.  ports vs expose: разница
    3.  Форматы ports
    4.  0.0.0.0 vs 127.0.0.1: где слушает Docker
    5.  Как проверить, что реально опубликовано
    6.  expose: документация, не защита
    7.  Внутренние сервисы: Postgres, Redis, Kafka
    8.  Что публиковать наружу
    9.  UDP и диапазоны
    10. Случайные порты
    11. Ошибки новичков
    12. Антипаттерны
    13. Финальные выводы

  1. ПРОБЛЕМА: ПОЧЕМУ ПОРТЫ — ЭТО ДЫРА
  Типичная ошибка новичка. Пишет compose для локальной разработки:
    services:
      postgres:
        image: postgres:16-alpine
        ports:
          - "5432:5432"
  Работает. Подключается psql'ом с хоста. Удобно.

  А потом этот же compose.yaml деплоится на VPS. С публичным
  IP. И что происходит:

    • Postgres слушает 0.0.0.0:5432.
    • Сканеры интернета находят открытый порт за минуты.
    • Начинают перебирать пароли (brute-force).
    • Если пароль слабый — взлом за часы.
    • Данные сливаются, база становится частью ботнета.

  Это реальные случаи. Не теория. Каждый день.

  ЕЩЁ ХУЖЕ С REDIS:
    services:
      redis:
        image: redis:7-alpine
        ports:
          - "6379:6379"

  У Redis по умолчанию **нет пароля**. Открытый Redis в интернете —
  это полный доступ к данным. Плюс возможность записать свой
  SSH-ключ в ~/.ssh/authorized_keys через дефолтную директорию
  (если он смонтирован). Классическая RCE.

  ВЫВОД: внутренние сервисы наружу публиковать НЕЛЬЗЯ.
  Только API-gateway / nginx / frontend.

  2. PORTS VS EXPOSE: РАЗНИЦА
  Две инструкции, которые выглядят похоже, но делают совершенно
  разное.

  PORTS — публикация наружу:
    services:
      nginx:
        ports:
          - "8080:80"

  Что происходит:
    • На хосте открывается порт 8080.
    • Docker слушает на нём (по дефолту на 0.0.0.0).
    • Трафик форвардится в контейнер на порт 80.
    • Доступно всему хосту и, если хост публичный, всему интернету.

  EXPOSE — только внутри Compose-сети:
    services:
      postgres:
        expose:
          - "5432"

  Что происходит:
    • Порт 5432 доступен другим контейнерам в той же сети.
    • На хосте порт НЕ открывается.
    • Снаружи (с твоего ноутбука) — не подключиться.
    • Из контейнера app — работает: `postgres:5432`.

  СХЕМА:
    ХОСТ                                    ИНТЕРНЕТ
    ┌─────────────────────────────────────┐
    │                                     │
    │  Compose-сеть                       │
    │  ┌──────────┐  ┌──────────┐         │
    │  │ nginx    │──│ app      │         │
    │  │ :80      │  │ :8080    │         │
    │  └────┬─────┘  └────┬─────┘         │
    │       │             │               │
    │       │             │ expose:8080   │
    │       │             │ (только сеть) │
    │  ports: "8080:80"                   │
    │       │                             │
    │       ▼                             │
    │  ХОСТ:8080  ◄───────────────────────┼── из интернета
    │                                     │
    └─────────────────────────────────────┘

  ПРАВИЛО:
    • ports — только для точки входа (nginx, API-gateway).
    • expose — для всего внутреннего (app, БД, кэш).
    • Ничего — тоже вариант, если порт не важен.

  3. ФОРМАТЫ PORTS
  Пять способов записать порт. Выбор зависит от задачи.

  3.1. ХОСТ:КОНТЕЙНЕР — БАЗОВЫЙ
    ports:
      - "8080:80"

  Хост-порт 8080, порт в контейнере 80. На хосте открыт 8080,
  снаружи стучишься на 8080.

  3.2. ТОЛЬКО КОНТЕЙНЕР — СЛУЧАЙНЫЙ ХОСТ-ПОРТ
    ports:
      - "80"

  Docker сам выберет свободный порт на хосте. Узнать его:
    docker compose port nginx 80
    # 0.0.0.0:32768

  Полезно, когда параллельно запущено 5 стеков, и хочется
  избежать конфликта портов.

  3.3. ЯВНЫЙ IP ХОСТА — 127.0.0.1
    ports:
      - "127.0.0.1:5432:5432"

  Порт открыт только на интерфейсе 127.0.0.1. Из интернета —
  не доступен. С самого хоста (localhost) — доступен.

  ЭТО САМЫЙ БЕЗОПАСНЫЙ ВАРИАНТ для внутренних сервисов,
  которым нужен доступ с хоста (например, psql, redis-cli).

  3.4. ЯВНЫЙ IP — 0.0.0.0
    ports:
      - "0.0.0.0:8080:80"

  Порт открыт на всех интерфейсах. Доступен из интернета, если
  у хоста публичный IP.

  По умолчанию (если не указывать IP) — именно так. Это то,
  что часто становится проблемой.

  3.5. КОНКРЕТНЫЙ IP ХОСТА
    ports:
      - "192.168.1.100:8080:80"

  Открыт только на одном интерфейсе с этим IP. Полезно на
  многосетевых хостах.

  ТАБЛИЦА:
    Запись                        | Что открыто
    -----------------------------|--------------------------------
    "8080:80"                    | 0.0.0.0:8080 (все интерфейсы)
    "127.0.0.1:8080:80"          | localhost:8080 (только хост)
    "192.168.1.100:8080:80"      | только этот IP
    "80"                         | 0.0.0.0:случайный
    "127.0.0.1:5432:5432"        | localhost:5432 (безопасно)

  4. 0.0.0.0 VS 127.0.0.1: ГДЕ СЛУШАЕТ DOCKER
  Ключевое различие, которое надо понимать на уровне сети.

  ЧТО ТАКОЕ 0.0.0.0:

    Специальный IP-адрес. Означает «все интерфейсы хоста».

    Если хост имеет интерфейсы:
      • lo (127.0.0.1) — локальный.
      • eth0 (публичный IP, например 203.0.113.5).
      • docker0 (172.17.0.1) — внутренний Docker.

    То 0.0.0.0 = слушать на всех них. Порт доступен:
      • С самого хоста через 127.0.0.1:8080.
      • Из локальной сети через 203.0.113.5:8080.
      • Из интернета через 203.0.113.5:8080.
      • Из Docker-контейнеров через 172.17.0.1:8080.

  ЧТО ТАКОЕ 127.0.0.1:
    Локальный loopback-интерфейс. Слушать только на нём.

    Порт доступен:
      • Только с самого хоста через 127.0.0.1:8080.
      • Из Docker-контейнеров — по особому маршруту
        (host.docker.internal на macOS/Windows).
      • Из интернета — НЕТ.
      • Из локальной сети — НЕТ.

  ПРАКТИЧЕСКИЙ ПРИМЕР:

    Порты без IP (плохо):
      postgres:
        ports:
          - "5432:5432"

    На VPS с IP 203.0.113.5. Проверка:
      nmap 203.0.113.5
      # PORT     STATE   SERVICE
      # 5432/tcp open    postgresql    ← доступен всему интернету

    Порты с 127.0.0.1 (хорошо):
      postgres:
        ports:
          - "127.0.0.1:5432:5432"

    Проверка:
      nmap 203.0.113.5
      # 5432/tcp closed  postgresql    ← не доступен

      psql -h 127.0.0.1 -p 5432 -U app
      # работает с хоста

  ПРАВИЛО:
    • Точка входа (nginx, API-gateway) → 0.0.0.0 (по умолчанию).
    • Внутренние сервисы с доступом с хоста → 127.0.0.1.
    • Внутренние сервисы без доступа с хоста → expose.

  5. КАК ПРОВЕРИТЬ, ЧТО РЕАЛЬНО ОПУБЛИКОВАНО
  Несколько способов убедиться, что порт не торчит куда не надо.

  5.1. DOCKER COMPOSE PS
    docker compose ps
    # NAME          PORTS
    # nginx-1       0.0.0.0:8080->80/tcp
    # postgres-1    127.0.0.1:5432->5432/tcp

  Если видишь `0.0.0.0:XXX` — доступен из интернета.
  Если `127.0.0.1:XXX` — только с хоста.
  Если ничего — не опубликован.

  5.2. DOCKER COMPOSE PORT
    docker compose port nginx 80
    # 0.0.0.0:8080

    docker compose port postgres 5432
    # 127.0.0.1:5432

  5.3. NETSTAT / SS НА ХОСТЕ
    # Linux/macOS.
    ss -tlnp | grep 5432
    # LISTEN  0  128  127.0.0.1:5432  ...

    # Windows PowerShell.
    netstat -ano | Select-String ":5432"
    # TCP    127.0.0.1:5432    0.0.0.0:0    LISTENING

  Если видишь `0.0.0.0:5432` или `*:5432` — плохо.
  Если `127.0.0.1:5432` — хорошо.

  5.4. NMAP С ДРУГОЙ МАШИНЫ
    Самый честный тест. С другого компьютера в той же сети:
      nmap <IP хоста>
      # PORT     STATE   SERVICE
      # 22/tcp   open    ssh
      # 80/tcp   open    http
      # 5432/tcp closed  postgresql   ← не доступен, правильно
  Если видишь 5432 открытым снаружи — срочно убирай ports.

  5.5. DOCKER INSPECT
    docker inspect <container> -f '{{json .NetworkSettings.Ports}}'
    # {"5432/tcp":[{"HostIp":"127.0.0.1","HostPort":"5432"}]}

    Если HostIp = "" или "0.0.0.0" — открыт везде.

  6. EXPOSE: ДОКУМЕНТАЦИЯ, НЕ ЗАЩИТА
  Важный момент, который часто путают.

  ЧТО EXPOSE РЕАЛЬНО ДЕЛАЕТ:
    • Помечает порт как «документированный» в метаданных образа.
    • В `docker ps` порт не появляется в колонке PORTS.
    • Некоторые инструменты (Traefik, k8s) используют это для
      автодискавери.

  ЧТО EXPOSE НЕ ДЕЛАЕТ:
    • Не блокирует порт.
    • Не защищает от подключения других контейнеров.
    • Не закрывает порт снаружи (это делает отсутствие ports).

  КЛЮЧЕВОЕ:
    Между контейнерами в одной сети **все порты доступны**,
    независимо от expose. Expose — это не «открыть порт», это
    «уведомить о порте».

    Реальная защита снаружи — отсутствие ports.

  ПРИМЕР:
    services:
      postgres:
        image: postgres:16-alpine
        # Нет ни ports, ни expose.

      app:
        image: app:1.0
        # Может подключиться к postgres:5432.
        # Потому что они в одной сети, и Docker не блокирует.

  То есть expose нужен больше для читаемости и для инструментов.
  Безопасность обеспечивается тем, что **нет ports**.

  7. ВНУТРЕННИЕ СЕРВИСЫ: POSTGRES, REDIS, KAFKA
  Про эти сервисы — отдельно, потому что они самые лакомые для атак.

  POSTGRES:
    services:
      postgres:
        image: postgres:16-alpine
        expose:
          - "5432"
        # ports НЕТ.

    Доступен из app: `postgres:5432`. Снаружи — не доступен.

    Если нужен psql с хоста в dev:
      # compose.override.yml
      services:
        postgres:
          ports:
            - "127.0.0.1:5432:5432"

    В проде override не применяется — порт не публикуется.

  REDIS:
    services:
      redis:
        image: redis:7-alpine
        expose:
          - "6379"
        # ports НЕТ.

    У Redis нет пароля по умолчанию. Публиковать наружу —
    катастрофа. Redis имеет известные CVE, где через порт
    можно выполнить произвольный код.

    Если нужен redis-cli с хоста в dev:
      # compose.override.yml
      services:
        redis:
          ports:
            - "127.0.0.1:6379:6379"

  KAFKA:
    services:
      kafka:
        image: apache/kafka:3.7.0
        expose:
          - "9092"
        # ports НЕТ.

    Kafka с открытым портом — утечка всех событий. Плюс
    возможность писать мусор в топики.

    Наружу нужен только если внешний сервис (за пределами
    Compose) читает/пишет в Kafka. Тогда:

      ports:
        - "127.0.0.1:9092:9092"

    И настройка advertised listeners так, чтобы Kafka отдавала
    правильный адрес.

  8. ЧТО ПУБЛИКОВАТЬ НАРУЖУ
  Общее правило: наружу публикуется только точка входа.

  ТОЧКА ВХОДА:
    services:
      nginx:
        ports:
          - "80:80"
          - "443:443"

    services:
      traefik:
        ports:
          - "80:80"
          - "443:443"

    services:
      api-gateway:
        ports:
          - "8080:8080"

  ВСЁ ОСТАЛЬНОЕ — ВНУТРИ:
    services:
      app:
        expose:
          - "8080"

      postgres:
        expose:
          - "5432"

      redis:
        expose:
          - "6379"

  СХЕМА:
    Интернет
       ↓
    nginx :443 (публичный)
       ↓
    app :8080 (внутри)
       ↓
    postgres :5432 / redis :6379 (внутри)

  Если у тебя микрофронтенд с 5 сервисами, наружу торчит
  только один nginx. Остальные — внутри.

  9. UDP И ДИАПАЗОНЫ

  UDP-ПОРТЫ:
    ports:
      - "53:53/udp"            # DNS
      - "51820:51820/udp"      # WireGuard
      - "500:500/udp"          # IPsec

  Если порт слушает и TCP, и UDP — указывай оба:
    ports:
      - "53:53/tcp"
      - "53:53/udp"

  ДИАПАЗОНЫ:
    ports:
      - "8000-8010:8000-8010"

  Открывает диапазон. Внутри контейнера сервис должен слушать
  на каждом порту в диапазоне.

  Для UDP-диапазона:
    ports:
      - "5000-5010:5000-5010/udp"

  ПРАВИЛО: открывать диапазоны — редко нужно. Обычно лучше
  явные порты, чтобы понимать, что и зачем.

  10. СЛУЧАЙНЫЕ ПОРТЫ

  Если указать только контейнерный порт:
    services:
      app:
        ports:
          - "8080"

  Docker выделит свободный порт на хосте. Узнать, какой:
    docker compose port app 8080
    # 0.0.0.0:32768

  Или:
    docker compose ps
    # NAME       PORTS
    # app-1      0.0.0.0:32768->8080/tcp

  КОГДА ПОЛЕЗНО:
    • Интеграционные тесты. Параллельно запускаются 5 стеков,
      каждому нужен свой порт. Docker сам разберётся.
    • Разработка. Свой сервис на 8080, у коллеги тот же код
      на том же 8080. Не конфликтует.
    • CI. Каждый job поднимает свой стек, порты не пересекаются.

  ПРИМЕР С ИНТЕГРАЦИОННЫМИ ТЕСТАМИ:
    # compose.test.yml
    services:
      app:
        ports:
          - "8080"    # случайный порт хоста
        expose:
          - "8080"

    После up:
      PORT=$(docker compose port app 8080 | cut -d: -f2)
      curl http://localhost:$PORT/health

  11. ОШИБКИ НОВИЧКОВ

  11.1. ПОРТЫ У ВСЕХ СЕРВИСОВ.
    Postgres, Redis, Kafka, MySQL, Mongo — всё наружу. Полный
    набор для взлома.

  11.2. 0.0.0.0 ПО УМОЛЧАНИЮ НА ПРОДЕ.
    Не указывают IP — Docker открывает 0.0.0.0. На VPS с
    публичным IP — доступно всем.

  11.3. ОДИН И ТОТ ЖЕ ПОРТ НА ХОСТЕ ДЛЯ ДВУХ СЕРВИСОВ.
    services:
      app1:
        ports: ["8080:8080"]
      app2:
        ports: ["8080:8080"]   # ← ошибка

    Ошибка: «port is already allocated». Меняй хост-порт или
    используй случайный.

  11.4. ЗАБЫЛИ ПРО UDP.
    "53:53" вместо "53:53/udp". DNS не работает.

  11.5. CONFUSION С 127.0.0.1 ВНУТРИ КОНТЕЙНЕРА.
    Сервис слушает на 127.0.0.1:8080 внутри контейнера.
    Другие контейнеры не могут достучаться.
    Правильно: сервис слушает на 0.0.0.0:8080 внутри
    контейнера, а публикация ограничивается через
    "127.0.0.1:8080:8080" в compose.

  11.6. EXPOSE КАК ЗАЩИТА.
    Думают, что expose = защита. Это документация, не более.
    Реальная защита — отсутствие ports.

  11.7. ПРОБРОС ЧЕРЕЗ IP ХОСТА НА 127.0.0.1 В КОНТЕЙНЕРЕ.
    "127.0.0.1:5432:5432" в compose означает **хост** 127.0.0.1,
    а не контейнер. Внутри контейнера сервис всё равно должен
    слушать 0.0.0.0.

  11.8. ПУБЛИКАЦИЯ НА КОНКРЕТНЫЙ ВНЕШНИЙ IP БЕЗ ПОНИМАНИЯ.
    "203.0.113.5:8080:80" — работает, только если IP реально
    есть на хосте. Иначе Docker не запустит контейнер.

  12. АНТИПАТТЕРНЫ
  12.1. POSTGRES/ REDIS/KAFKA/MONGO НАРУЖУ.
    Только expose. Никогда ports в проде.
  12.2. 0.0.0.0:XXX ДЛЯ ВНУТРЕННИХ СЕРВИСОВ.
    "5432:5432" = 0.0.0.0:5432. Используй 127.0.0.1.
  12.3. EXPOSE ДЛЯ ЗАЩИТЫ.
    Expose — документация. Реальная защита — отсутствие ports.
  12.4. LATEST НА ПОРТАХ.
    Порт не должен меняться между окружениями. Фиксируй в .env.
  12.5. ХАРДКОД ПОРТОВ.
    APP_PORT=8080 в yaml. Через месяц надо 9090 — правишь
    везде. Используй ${APP_PORT}.
  12.6. ОДИН ПОРТ ДЛЯ DEV И PROD.
    В dev psql с хоста на 5432, в prod — тот же 5432 наружу.
    Разные override-файлы.
  12.7. RANDOM ПОРТ БЕЗ СПОСОБА УЗНАТЬ.
    Если не читаешь `docker compose port`, теряешь
    возможность подключиться.
  12.8. UDP ЗАБЫЛИ.
    "53:53" для DNS — не работает. Нужен /udp.
  12.9. ПОРТЫ В GIT.
    Если у каждого разработчика свои порты — пусть их
    задают в .env, а в compose.yaml только ${VAR}.
  12.10. ОТКРЫТЫЙ IP ХОСТА БЕЗ FIREWALL.
    Даже если 127.0.0.1 в compose — проверь, что фаервол
    хоста не пропускает эти порты снаружи.

  13. ФИНАЛЬНЫЕ ВЫВОДЫ
  1.  ports — публикует наружу. expose — только внутри
      Compose-сети. Разница критична для безопасности.
  2.  postgres: "5432:5432" = 0.0.0.0:5432 = доступен
      всему интернету. Плохо.
  3.  127.0.0.1:5432:5432 — только с хоста. Безопасно.
      Для внутренних сервисов с доступом с хоста.
  4.  expose — документация, не защита. Реальная защита —
      отсутствие ports.
  5.  Наружу торчит только точка входа: nginx, API-gateway, Traefik.
  6.  Postgres, Redis, Kafka, Mongo — только expose.
      Никогда ports в проде.
  7.  Для dev — ports с 127.0.0.1 в override-файле. В prod
      override не применяется.
  8.  Проверка: docker compose ps, docker compose port,
      netstat, nmap с другой машины.
  9.  UDP — указывать /udp. Диапазоны — "8000-8010:8000-8010".
  10. Случайные порты "8080" — для тестов и CI. Узнать:
      docker compose port.
  11. Ошибки новичков: ports у всех, 0.0.0.0 на проде,
      expose как защита, забыли UDP, одинаковый порт.
  12. Антипаттерны: Postgres наружу, 0.0.0.0 для внутренних,
      hardcoded порты, один порт для dev/prod.
*/
