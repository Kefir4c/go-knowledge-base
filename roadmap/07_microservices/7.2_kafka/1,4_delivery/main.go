package main

/*
  БЛОК 1.4: ГАРАНТИИ ДОСТАВКИ (DELIVERY SEMANTICS)
  Гарантии доставки (Delivery Semantics) — это фундаментальный аспект
  работы с Kafka, определяющий, сколько раз сообщение может быть доставлено
  и обработано. В распределённых системах неизбежны сбои: сеть может
  оборваться, брокер упасть, консюмер зависнуть. Гарантии доставки
  описывают поведение системы в таких условиях.

  СОДЕРЖАНИЕ:
    1.  Что такое гарантии доставки и почему они важны
    2.  At-most-once (максимум один раз)
    3.  At-least-once (минимум один раз) — стандарт в Kafka
    4.  Exactly-once (ровно один раз) — высшая гарантия
    5.  Effectively Once — реалистичный подход в распределённых системах
    6.  Как достичь exactly-once: идемпотентность + транзакции
    7.  Идемпотентный продюсер — механизм (и дефолт в Kafka 3.0+)
    8.  Транзакции в Kafka — атомарность
    9.  Гарантии на стороне консюмера
    10. Exactly-once — локальная гарантия, не распространяется на внешние системы
    11. Выбор семантики для разных сценариев
    12. Ключевые выводы для собеседования

  1.  ЧТО ТАКОЕ ГАРАНТИИ ДОСТАВКИ И ПОЧЕМУ ОНИ ВАЖНЫ
  Гарантии доставки определяют, что произойдёт с сообщением в случае сбоя:
    • Будет ли оно потеряно?
    • Будет ли оно обработано повторно?
    • Будет ли оно доставлено ровно один раз?

  Это критически важно для разных типов приложений:
    • Финансовые системы — не допускают потерь или дублей (exactly-once).
    • Системы мониторинга — потеря нескольких метрик допустима (at-most-once).
    • Платёжные системы — дубли недопустимы (exactly-once).
    • Аналитика — допустимы дубли, важна скорость (at-least-once).

  2.  AT-MOST-ONCE (МАКСИМУМ ОДИН РАЗ)
  Определение: сообщение может быть потеряно, но никогда не будет обработано
  дважды.

  КАК ЭТО РАБОТАЕТ:
    • Продюсер: acks=0 (не ждёт подтверждения) — сообщение считается
      отправленным сразу.
    • Консюмер: коммит смещения происходит ДО обработки сообщения.
      Если консюмер падает после коммита, но до обработки,
      сообщение считается прочитанным и теряется.

  ПРИМЕР КОДА (консюмер):
    records = consumer.poll()
    consumer.commitSync()          // коммит смещения ДО обработки
    process(records)               // обработка может упасть

  ПРЕИМУЩЕСТВА:
    • Максимальная пропускная способность (нет задержек на ожидание).
    • Минимальные накладные расходы.

  НЕДОСТАТКИ:
    • Потеря данных гарантирована при сбоях.
    • Не подходит для критичных данных.

  КОГДА ИСПОЛЬЗОВАТЬ:
    • Данные, где потеря допустима (например, метрики, логи).
    • Системы с крайне высокой нагрузкой, где скорость важнее надёжности.
    • Тестирование и разработка.

  3.  AT-LEAST-ONCE (МИНИМУМ ОДИН РАЗ) — СТАНДАРТ В KAFKA
  Определение: сообщение никогда не будет потеряно, но может быть обработано
  повторно.

  КАК ЭТО РАБОТАЕТ:
    • Продюсер: acks=1 или acks=all (ждать подтверждения от лидера/ISR).
      Сообщение не теряется при сбое лидера, если есть реплики в ISR.
    • Консюмер: коммит смещения происходит ПОСЛЕ обработки сообщения.
      Если консюмер падает до коммита, сообщение будет обработано повторно.

  ПРИМЕР КОДА (консюмер):
    records = consumer.poll()
    process(records)               // обработка
    consumer.commitSync()          // коммит смещения ПОСЛЕ обработки

  ПРЕИМУЩЕСТВА:
    • Надёжность — данные не теряются.
    • Стандартная гарантия в Kafka.
    • Легко реализовать.

  НЕДОСТАТКИ:
    • Возможны дубли сообщений (при сбое консюмера после обработки,
      но до коммита).
    • Требует идемпотентности обработки.

  КОГДА ИСПОЛЬЗОВАТЬ:
    • Большинство систем (стандартный выбор).
    • Когда дубли допустимы или обработка идемпотентна.
    • Когда важна надёжность, но exactly-once слишком дорог.

  4.  EXACTLY-ONCE (РОВНО ОДИН РАЗ) — ВЫСШАЯ ГАРАНТИЯ
  Определение: каждое сообщение будет обработано ровно один раз,
  без потерь и без дублей.

  КАК ЭТО РАБОТАЕТ:
    • Требует согласованной работы продюсера, консюмера и брокеров.
    • Продюсер: идемпотентный (enable.idempotence=true) + транзакции.
    • Консюмер: ручной коммит смещения в той же транзакции, что и обработка.
    • Брокер: min.insync.replicas + acks=all.

  ПРИМЕР КОДА (консюмер + продюсер в транзакции):
    producer.initTransactions()
    producer.beginTransaction()

    records = consumer.poll()
    for record in records:
        result = process(record)
        producer.send(result)               // отправка результата в другой топик

    // Коммит смещения консюмера в той же транзакции
    producer.sendOffsetsToTransaction(offsets, consumerGroup)

    producer.commitTransaction()            // атомарный коммит

  ПРЕИМУЩЕСТВА:
    • Гарантирует, что данные не будут потеряны и не будут дублироваться.
    • Позволяет строить системы с точно-один раз для финансов, платёжных
      систем и других критичных приложений.

  НЕДОСТАТКИ:
    • Существенное снижение пропускной способности (в 2-3 раза).
    • Сложность реализации.
    • Требует поддержки от брокеров (версия Kafka 0.11+).

  КОГДА ИСПОЛЬЗОВАТЬ:
    • Платёжные системы.
    • Финансовые транзакции.
    • Бухгалтерские операции.
    • Любые системы, где дубли или потери недопустимы.

  5.  EFFECTIVELY ONCE — РЕАЛИСТИЧНЫЙ ПОДХОД В РАСПРЕДЕЛЁННЫХ СИСТЕМАХ
  На практике достичь абсолютной exactly-once (без дублей и потерь) в любой
  распределённой системе невозможно из-за сетевых сбоев, перезапусков,
  асинхронности и других факторов. Поэтому в индустрии часто говорят об
  «effectively once» (эффективно один раз).

  Суть подхода:
    • Kafka гарантирует, что внутри платформы сообщение будет записано
      ровно один раз (благодаря идемпотентному продюсеру и транзакциям).
    • Однако на стороне консюмера могут возникать дубли из-за повторных
      вызовов (например, при сбое консюмера после обработки, но до коммита).
    • В таких случаях финальная защита от дублей обеспечивается
      на уровне бизнес-логики — с помощью идемпотентности операций
      или дедупликации по уникальному ID сообщения.

  Практическая реализация effectively once:
    • Сохраняйте уникальный ID сообщения (например, UUID) в бизнес-данных.
    • При обработке проверяйте, не обработано ли уже это сообщение
      (например, по ID в базе данных).
    • Если сообщение уже обработано — пропускайте его (идемпотентность).

  Это даёт возможность получать практически exactly-once с минимальными
  накладными расходами и без необходимости использования транзакций.

  6.  КАК ДОСТИЧЬ EXACTLY-ONCE: ИДЕМПОТЕНТНОСТЬ + ТРАНЗАКЦИИ
  Exactly-once в Kafka достигается комбинацией двух механизмов:

  6.1. Идемпотентный продюсер (enable.idempotence=true)
    Гарантирует, что даже при повторных отправках одного и того же сообщения
    оно будет записано в партицию только один раз.

    Как работает:
      • Каждому продюсеру присваивается уникальный PID (Producer ID).
      • Каждое сообщение получает порядковый номер (sequence number)
        в пределах партиции.
      • Брокер проверяет, что сообщение с таким номером ещё не было записано.
      • Если уже было — отклоняет дубликат.

  6.2. Транзакции (transactional.id)
    Позволяют продюсеру отправлять сообщения в несколько партиций атомарно:
      • Все сообщения записываются, либо ни одного.
      • Связывают коммит смещения консюмера с транзакцией продюсера.

    Настройки:
      • transactional.id = "my-transactional-id"
      • isolation.level = read_committed (для консюмера)

  6.3. End-to-end exactly-once
    Для сквозной exactly-once обработки (от продюсера через Kafka к консюмеру)
    нужно:
      • Идемпотентный продюсер.
      • Транзакции на стороне продюсера.
      • Консюмер с ручным коммитом и чтением только закоммиченных сообщений.
      • Атомарный коммит смещения консюмера в той же транзакции.

  7.  ИДЕМПОТЕНТНЫЙ ПРОДЮСЕР — МЕХАНИЗМ (И ДЕФОЛТ В KAFKA 3.0+)
  Идемпотентность — это свойство операции, при котором её повторное
  применение не изменяет состояние системы.

  В KAFKA:
    • Включается: enable.idempotence=true
    • С версии Kafka 3.0 идемпотентность включена ПО УМОЛЧАНИЮ
      (enable.idempotence=true).
    • PID (Producer ID) — уникальный идентификатор продюсера.
    • Sequence Number — порядковый номер сообщения в партиции.

  КАК ЭТО РАБОТАЕТ НА ПРАКТИКЕ:

    1. Продюсер инициализируется и получает PID от брокера.
    2. При отправке сообщения продюсер добавляет:
       - PID
       - Sequence Number (увеличивается для каждой партиции).
    3. Брокер проверяет последний записанный sequence number для этого PID.
    4. Если новый sequence number == last + 1 → записывает.
    5. Если sequence number уже был записан → возвращает успех
       (дубликат игнорируется).
    6. Если sequence number > last + 1 → ошибка (пропуск).

  ЭТО ДАЁТ:
    • Гарантию, что каждое сообщение будет записано только один раз,
      даже при повторных попытках отправки.

  8.  ТРАНЗАКЦИИ В KAFKA — АТОМАРНОСТЬ
  Транзакции в Kafka позволяют выполнять атомарные операции
  (все или ничего).

  КЛЮЧЕВЫЕ КОМПОНЕНТЫ:
    • Transactional Producer — продюсер с transactional.id.
    • Transaction Coordinator — брокер, управляющий транзакциями.
    • Transaction Log — внутренний топик __transaction_state.

  ЖИЗНЕННЫЙ ЦИКЛ ТРАНЗАКЦИИ:
    1. initTransactions() — инициализация.
    2. beginTransaction() — начало транзакции.
    3. send() — отправка сообщений в рамках транзакции.
    4. commitTransaction() — фиксация (атомарно записываются все сообщения).
    5. abortTransaction() — откат (удаляет все сообщения транзакции).

  КАК ЭТО РАБОТАЕТ:
    • Транзакция привязывается к producer ID и transactional.id.
    • Все сообщения, отправленные в транзакции, получают метку транзакции.
    • Брокеры видят, что сообщения находятся в транзакции, и не показывают их
      консюмерам до commit.

  ИЗОЛЯЦИЯ ДЛЯ КОНСЮМЕРА:
    • isolation.level = read_uncommitted (по умолчанию) — читает все сообщения,
      включая незакоммиченные.
    • isolation.level = read_committed — читает только закоммиченные
      транзакции.

  9.  ГАРАНТИИ НА СТОРОНЕ КОНСЮМЕРА
  Гарантии доставки также зависят от поведения консюмера.

  9.1. At-most-once (консюмер)

    • enable.auto.commit = true (или ручной коммит ДО обработки).
    • При сбое после коммита сообщение теряется.

  9.2. At-least-once (консюмер)

    • enable.auto.commit = false.
    • Ручной коммит ПОСЛЕ обработки.
    • При сбое до коммита сообщение повторяется.

  9.3. Exactly-once (консюмер)

    • enable.auto.commit = false.
    • Ручной коммит в той же транзакции, что и отправка результата.
    • Используется producer.sendOffsetsToTransaction().

  ВАЖНО: exactly-once на стороне консюмера требует exactly-once на стороне
  продюсера (идемпотентность + транзакции).

  10. EXACTLY-ONCE — ЛОКАЛЬНАЯ ГАРАНТИЯ
  Критически важно понимать: exactly-once в Kafka работает **внутри**
  экосистемы Kafka. Если после обработки сообщения вы обновляете внешнюю
  базу данных (PostgreSQL, MySQL) или вызываете внешний API, гарантия
  exactly-once **не распространяется** на эти внешние операции.

  ПРОБЛЕМА:
    • Консюмер прочитал сообщение, обновил внешнюю БД, но до коммита смещения
      в Kafka произошёл сбой.
    • При восстановлении консюмер прочитает то же сообщение повторно
      (at-least-once), и внешняя БД получит дублирующее обновление.
    • Кафка не знает о состоянии внешней системы, поэтому не может
      гарантировать exactly-once для такого сценария.

  РЕШЕНИЯ:
    1. Паттерн Transactional Outbox:
       • Сохраняйте событие для Kafka в той же транзакции, что и обновление
         внешней БД.
       • Отдельный воркер читает outbox и отправляет в Kafka.
       • При сбое воркер перечитает outbox и отправит повторно (at-least-once)
         — но дублей не будет, потому что outbox-запись уже закоммичена.
    2. Идемпотентность внешних вызовов:
       • Делайте обновления внешних систем идемпотентными (например, проверяйте
         уникальный ID заказа перед вставкой).
       • Повторная обработка сообщения не приведёт к двойному изменению.

  11. ВЫБОР СЕМАНТИКИ ДЛЯ РАЗНЫХ СЦЕНАРИЕВ
  ┌───────────────────────┬─────────────────────────────────────────────────┐
  │ Сценарий              │ Рекомендуемая семантика                         │
  ├───────────────────────┼─────────────────────────────────────────────────┤
  │ Системы мониторинга   │ At-most-once (скорость важнее точности)         │
  ├───────────────────────┼─────────────────────────────────────────────────┤
  │ Big Data / Analytics  │ At-least-once (дубли допустимы)                 │
  ├───────────────────────┼─────────────────────────────────────────────────┤
  │ Обновление кэша       │ At-least-once (идемпотентная обработка)         │
  ├───────────────────────┼─────────────────────────────────────────────────┤
  │ Платёжные системы     │ Exactly-once (дубли недопустимы)                │
  ├───────────────────────┼─────────────────────────────────────────────────┤
  │ Банковские транзакции │ Exactly-once (требуется строгое соблюдение)     │
  ├───────────────────────┼─────────────────────────────────────────────────┤
  │ Системы заказов       │ Exactly-once или at-least-once с проверкой      │
  │                       │ дублей на уровне приложения                     │
  └───────────────────────┴─────────────────────────────────────────────────┘

  12. КЛЮЧЕВЫЕ ВЫВОДЫ ДЛЯ СОБЕСЕДОВАНИЯ
  1.  At-most-once — потеря данных допустима, дублей нет.
      acks=0, коммит до обработки. Для некритичных данных.
  2.  At-least-once — данные не теряются, но возможны дубли.
      acks=1/all, коммит после обработки. Стандарт в Kafka.
  3.  Exactly-once — каждое сообщение ровно один раз.
      Идемпотентный продюсер (enable.idempotence=true) + транзакции.
  4.  Идемпотентный продюсер — PID + sequence number,
      защита от дублей в партиции.
  5.  С Kafka 3.0 идемпотентный продюсер включён по умолчанию.
  6.  Транзакции — атомарная запись в несколько партиций,
      связь коммита смещения консюмера с транзакцией.
  7.  Консюмер:
      - Для at-least-once: commit после обработки.
      - Для exactly-once: commit в транзакции.
  8.  isolation.level = read_committed позволяет читать только
      закоммиченные транзакции.
  9.  Exactly-once снижает производительность (на 30-50%),
      но даёт высшую надёжность.
  10. Выбор семантики — компромисс между скоростью и надёждностью.
  11. Exactly-once — локальная гарантия в пределах Kafka.
      Для внешних систем используйте Transactional Outbox или идемпотентность.
  12. Практическая реализация "effectively once" достигается с помощью
      идемпотентной обработки на уровне бизнес-логики.
*/
