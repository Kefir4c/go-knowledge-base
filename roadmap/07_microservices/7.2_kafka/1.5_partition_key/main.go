package partitionkey

/*
  БЛОК 1.5: PARTITION KEY И ПОРЯДОК СООБЩЕНИЙ
  Порядок сообщений — один из самых частых вопросов на собеседованиях,
  потому что в Kafka он гарантируется только при правильной настройке
  партиционирования. Непонимание этой концепции приводит к серьёзным
  архитектурным ошибкам.

  СОДЕРЖАНИЕ:
    1.  Почему порядок важен
    2.  Порядок гарантируется только внутри партиции
    3.  Partition Key — основной механизм управления порядком
    4.  Хэш-функция и определение партиции
    5.  Отправка без ключа (null key) — round-robin
    6.  Добавление партиций — как это ломает порядок
    7.  Кастомные партиционеры — реализация на Go
    8.  Влияние на параллелизм консюмеров
    9.  Горячие партиции (hot partition) и как с ними бороться
    10. Стратегии выбора ключа
    11. Практические рекомендации и подводные камни
    12. Ключевые выводы для собеседования

  1.  ПОЧЕМУ ПОРЯДОК ВАЖЕН
  Во многих бизнес-сценариях важен порядок обработки событий:
    • События по одному заказу: создание → оплата → доставка.
    • Действия пользователя: вход → покупка → выход.
    • Финансовые транзакции по счёту: начисление → списание.

  Если такие события обрабатываются в неправильном порядке, бизнес-логика
  нарушается. Например, списание средств до их начисления.

  2.  ПОРЯДОК ГАРАНТИРУЕТСЯ ТОЛЬКО ВНУТРИ ПАРТИЦИИ
  Это фундаментальное правило Kafka.

  ОСОБЕННОСТИ:
    • В пределах одной партиции сообщения строго упорядочены по offset.
    • Порядок между разными партициями НЕ ГАРАНТИРУЕТСЯ.
    • Если два сообщения попадают в разные партиции, они могут обрабатываться
      параллельно и в любом порядке.

  ПРИМЕР:
    Топик orders (3 партиции: P0, P1, P2)
    Сообщение A (заказ 1) → P0, смещение 5
    Сообщение B (заказ 1) → P1, смещение 3
    Консюмеры обрабатывают P0 и P1 параллельно — порядок A и B не определён.

  ВЫВОД: чтобы гарантировать порядок для определённого набора событий,
  все они должны попадать в одну партицию.

  3.  PARTITION KEY — ОСНОВНОЙ МЕХАНИЗМ УПРАВЛЕНИЯ ПОРЯДКОМ
  Partition Key (ключ партиционирования) — это значение, которое продюсер
  указывает при отправке сообщения. Kafka использует этот ключ для
  определения партиции.

  КЛЮЧЕВОЙ ПРИНЦИП:
    • Все сообщения с одинаковым ключом всегда попадают в одну партицию.
    • Это гарантирует, что они будут обрабатываться в том порядке,
      в котором были отправлены.

  ПРИМЕР:
    key = "user_123" → всегда одна и та же партиция.
    Все события пользователя 123 → P1.
    Порядок гарантирован.

  ВЫБОР КЛЮЧА:
    • Для пользовательских событий: UserID.
    • Для заказов: OrderID.
    • Для сессий: SessionID.
    • Для устройств: DeviceID.

  4.  ХЭШ-ФУНКЦИЯ И ОПРЕДЕЛЕНИЕ ПАРТИЦИИ
  Kafka определяет номер партиции по следующей формуле:

    partition = hash(key) % number_of_partitions

  ГДЕ:
    • hash(key) — хэш-функция (по умолчанию Java hashCode для строк).
    • number_of_partitions — количество партиций в топике.

  ЭТО ОЗНАЧАЕТ:
    • Один и тот же ключ всегда даёт одно и то же значение при одинаковом
      количестве партиций.
    • При изменении количества партиций результат меняется.

  ПРИМЕР (3 партиции):
    key = "user_123", hash = 123456, 123456 % 3 = 0 → партиция 0.
    key = "user_456", hash = 789012, 789012 % 3 = 1 → партиция 1.

  ПРИМЕР (5 партиций):
    key = "user_123", 123456 % 5 = 1 → партиция 1.
    key = "user_456", 789012 % 5 = 2 → партиция 2.

  ВАЖНО: при добавлении партиций ключ может перескочить в другую партицию.

  5.  ОТПРАВКА БЕЗ КЛЮЧА (NULL KEY) — ROUND-ROBIN
  Если ключ не указан (null), Kafka не имеет информации для партиционирования.

  КАК РАБОТАЕТ:
    • Kafka использует round-robin (по кругу) или случайное распределение
      между партициями (зависит от версии).
    • Сообщения распределяются равномерно, но порядок не гарантируется.

  ПРИМЕР (3 партиции):
    Сообщение 1 → P0, сообщение 2 → P1, сообщение 3 → P2, сообщение 4 → P0, ...

  ПОСЛЕДСТВИЯ:
    • Сообщения от одного пользователя могут попасть в разные партиции.
    • Порядок их обработки не гарантируется.
    • Подходит для логов и метрик, где порядок не критичен.

  6.  ДОБАВЛЕНИЕ ПАРТИЦИЙ — КАК ЭТО ЛОМАЕТ ПОРЯДОК
  Добавление партиций — наиболее частая причина нарушения порядка в продакшене.

  КАК ЭТО РАБОТАЕТ:
    1. Было 3 партиции, key = "user_123" попадал в P0.
    2. Добавили 5 партиций (стало 5).
    3. Теперь hash % 5 даёт другое значение, ключ может попасть в P2.
    4. Сообщения до добавления → P0, после → P2.
    5. Порядок нарушен для всего, что идёт по этому ключу.

  ПОЧЕМУ ЭТО ПРОИСХОДИТ:
    • Формула partition = hash(key) % number_of_partitions зависит
      от общего количества партиций.
    • Существующие данные остаются на месте (не перемещаются).
    • Новые сообщения распределяются по новой логике.

  КАК ИЗБЕЖАТЬ:
    • Заранее планируйте количество партиций с запасом (×2, ×3).
    • Если нужно добавить партиции, используйте кастомный партиционер
      с фиксированным маппингом ключей (например, по диапазону).
    • Избегайте частого добавления партиций — это требует тщательного
      планирования.

  7.  КАСТОМНЫЕ ПАРТИЦИОНЕРЫ — РЕАЛИЗАЦИЯ НА GO
  Если стандартной хэш-функции недостаточно, можно реализовать свою логику
  распределения сообщений по партициям.

  КОГДА ИСПОЛЬЗОВАТЬ:
    • Сложные бизнес-правила (например, по географическому региону).
    • Фиксированный маппинг ключей (ключ → фиксированная партиция).
    • Учёт нагрузки на партиции (динамическое партиционирование).

  ПРИМЕР (фиксированный маппинг) на Java:
    class FixedPartitioner implements Partitioner {
        public int partition(String topic, Object key, byte[] keyBytes,
                             Object value, byte[] valueBytes, Cluster cluster) {
            if (key.equals("user_123")) return 0;
            if (key.equals("user_456")) return 1;
            return Math.abs(key.hashCode()) % cluster.partitionCountForTopic(topic);
        }
    }

  В Go (segmentio/kafka-go) нет встроенного кастомного партиционера,
  но можно использовать ключ и ручное указание партиции:

    // Ручное указание партиции через Message
    msg := kafka.Message{
        Topic: "orders",
        Key:   []byte("user_123"),
        Value: []byte(`{"event":"order_created"}`),
        // Partition: 0, // можно явно указать, но тогда ключ игнорируется
    }

  Для сложной логики лучше использовать обёртку:
    type CustomPartitioner struct {
        // логика
    }

    func (p *CustomPartitioner) Partition(topic string, key []byte) int {
        // пользовательская логика
        return 0 // фиксированная партиция для данного ключа
    }

  8.  ВЛИЯНИЕ НА ПАРАЛЛЕЛИЗМ КОНСЮМЕРОВ
  Количество партиций определяет максимальное количество консюмеров,
  которые могут читать параллельно.

  ВЛИЯНИЕ КЛЮЧА НА ПАРАЛЛЕЛИЗМ:
    • Если все сообщения идут с одним ключом, они попадают в одну партицию.
    • Тогда только один консюмер может обрабатывать их параллельно.
    • Это ограничивает пропускную способность.

  БАЛАНС:
    • Много ключей → данные распределяются по партициям → высокий параллелизм.
    • Один ключ → все сообщения в одной партиции → низкий параллелизм.

  ПРАКТИЧЕСКИЙ ВЫВОД:
    • Если важна производительность, используйте ключи, которые дают
      равномерное распределение (например, UserID).
    • Избегайте популярных ключей (hot key), которые создают горячую партицию.

  9.  ГОРЯЧИЕ ПАРТИЦИИ (HOT PARTITION) И КАК С НИМИ БОРОТЬСЯ
  Горячая партиция — это партиция, которая получает непропорционально
  большое количество сообщений из-за того, что один или несколько ключей
  очень популярны.

  ПРОБЛЕМЫ:
    • Перегрузка одного брокера.
    • Увеличение задержки.
    • Снижение общей производительности кластера.

  РЕШЕНИЯ:
    1. Составной ключ: key = user_id + timestamp (или user_id + salt).
       Это разбивает популярный ключ на несколько под-ключей,
       распределяя нагрузку по партициям.
    2. Случайная соль (salt): key = user_id + "_" + random_number.
       Позволяет распределить нагрузку, но нарушает порядок.
    3. Кастомный партиционер: учитывает нагрузку и перераспределяет ключи.
    4. Увеличение количества партиций: больше партиций = больше вместимость.

  10. СТРАТЕГИИ ВЫБОРА КЛЮЧА
  ┌───────────────────────┬─────────────────────────────────────────────────┐
  │ Сценарий              │ Рекомендуемый ключ                           │
  ├───────────────────────┼─────────────────────────────────────────────────┤
  │ События одного юзера  │ UserID (идентификатор пользователя)           │
  ├───────────────────────┼─────────────────────────────────────────────────┤
  │ События одного заказа │ OrderID (идентификатор заказа)               │
  ├───────────────────────┼─────────────────────────────────────────────────┤
  │ Транзакции по аккаунту│ AccountID (номер счёта)                      │
  ├───────────────────────┼─────────────────────────────────────────────────┤
  │ Сессионные события    │ SessionID (идентификатор сессии)             │
  ├───────────────────────┼─────────────────────────────────────────────────┤
  │ Логи и метрики        │ null (распределение по партициям)            │
  ├───────────────────────┼─────────────────────────────────────────────────┤
  │ Равномерное           │ random значение (или null)                   │
  │ распределение         │                                                 │
  └───────────────────────┴─────────────────────────────────────────────────┘

  11. ПРАКТИЧЕСКИЕ РЕКОМЕНДАЦИИ И ПОДВОДНЫЕ КАМНИ

  11.1. ВСЕГДА ИСПОЛЬЗУЙТЕ КЛЮЧ ДЛЯ ПОРЯДКА
    Если порядок важен, всегда указывайте ключ. Даже если вы не уверены,
    какой ключ использовать, лучше указать что-то, чем ничего.

  11.2. НЕ МЕНЯЙТЕ КОЛИЧЕСТВО ПАРТИЦИЙ БЕЗ ПЛАНИРОВАНИЯ
    Добавление партиций ломает порядок. Если это необходимо:
      • Проведите миграцию данных (перезапись в новый топик).
      • Используйте кастомный партиционер с фиксированным маппингом.

  11.3. МОНИТОРЬТЕ ГОРЯЧИЕ ПАРТИЦИИ
    Одна партиция с большим количеством сообщений (hot partition)
    ограничивает производительность. Используйте составные ключи
    для распределения.

  11.4. УЧИТЫВАЙТЕ КОНСЮМЕРОВ
    Количество партиций определяет максимальное количество консюмеров.
    Если партиций меньше, чем консюмеров, часть консюмеров будет
    простаивать.

  11.5. ИДЕМПОТЕНТНОСТЬ НЕ РЕШАЕТ ПРОБЛЕМУ ПОРЯДКА
    Идемпотентный продюсер защищает от дублей, но не гарантирует порядок.
    Если два сообщения с одним ключом отправлены в разных партициях,
    порядок может нарушиться.

  11.6. ПРИ ДОБАВЛЕНИИ ПАРТИЦИЙ ПЕРЕРАСПРЕДЕЛЕНИЯ ДАННЫХ НЕ ПРОИСХОДИТ
    Существующие сообщения не перемещаются — они остаются в старых партициях.
    Только новые сообщения будут распределяться по новой логике.
    Это может привести к дисбалансу данных.

  12. КЛЮЧЕВЫЕ ВЫВОДЫ ДЛЯ СОБЕСЕДОВАНИЯ
  1.  Порядок сообщений гарантируется ТОЛЬКО внутри одной партиции.
  2.  Partition Key — основной инструмент для управления порядком.
      Все сообщения с одинаковым ключом попадают в одну партицию.
  3.  Формула: partition = hash(key) % number_of_partitions.
  4.  Без ключа (null key) сообщения распределяются по кругу (round-robin),
      порядок не гарантируется.
  5.  Добавление партиций меняет хэш-функцию и может нарушить порядок
      для существующих ключей.
  6.  Если порядок важен, всегда указывайте ключ.
  7.  Количество партиций определяет максимальный параллелизм консюмеров.
      Много ключей → высокий параллелизм. Один ключ → низкий параллелизм.
  8.  Горячие партиции (hot partitions) — следствие неравномерного
      распределения ключей. Используйте составные ключи для балансировки.
  9.  Кастомные партиционеры позволяют реализовать собственную логику
      распределения (по географическому региону, по диапазону ключей).
  10. Планируйте количество партиций заранее, чтобы избежать добавления
      в продакшене.
  11. При добавлении партиций старые данные не перераспределяются —
      только новые сообщения.
  12. Идемпотентность не гарантирует порядок — она защищает только от дублей.
  13. Составные ключи (user_id + timestamp) помогают бороться с горячими
      партициями.
*/
