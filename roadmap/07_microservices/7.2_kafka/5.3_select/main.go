package selects

/*
  БЛОК 5.3: КОГДА ЧТО ИСПОЛЬЗОВАТЬ?
  Выбор правильного брокера сообщений — это одно из самых важных архитектурных
  решений. Неправильный выбор может привести к катастрофическим последствиям:
  перерасходу ресурсов, падению производительности, потере данных или
  неоправданной сложности.

  В этой теории мы разберём:
    1. Три кита: Kafka, RabbitMQ, NATS — их место в экосистеме
    2. Детальное сравнение по ключевым критериям
    3. Сценарии использования каждого брокера
    4. Как принимать решение: пошаговый алгоритм
    5. Антипаттерны и частые ошибки
    6. Миграция: когда и как переходить с одного на другой
    7. Ключевые выводы для собеседования

  1.  ТРИ КИТА: МЕСТО В ЭКОСИСТЕМЕ

  1.1. APACHE KAFKA — КОРОЛЬ СТРИМИНГА И СОБЫТИЙНОГО ХРАНЕНИЯ
    Kafka — это распределённый commit log. Она хранит все сообщения
    и позволяет воспроизводить их в любой момент.

    СИЛЬНЫЕ СТОРОНЫ:
      • Долгосрочное хранение (retention)
      • Replay сообщений
      • Exactly-once семантика
      • Огромная пропускная способность
      • Строгий порядок внутри партиций
      • Экосистема: Kafka Streams, Kafka Connect, Schema Registry

    СЛАБЫЕ СТОРОНЫ:
      • Сложность настройки и эксплуатации
      • Высокая задержка (по сравнению с NATS)
      • Не подходит для request-response (RPC)
      • Требует ZooKeeper или KRaft для управления

  1.2. RABBITMQ — КОРОЛЬ СЛОЖНОЙ МАРШРУТИЗАЦИИ И ЗАДАЧ
    RabbitMQ — это классический брокер сообщений с богатыми возможностями
    маршрутизации на основе протокола AMQP 0-9-1.

    СИЛЬНЫЕ СТОРОНЫ:
      • Сложная маршрутизация (4 типа exchange)
      • Поддержка множества протоколов (AMQP, MQTT, STOMP)
      • Отличная поддержка задач (Task Queues)
      • Низкая задержка
      • Простота настройки и эксплуатации

    СЛАБЫЕ СТОРОНЫ:
      • Нет долгосрочного хранения (сообщения удаляются после ack)
      • Нет replay
      • Нет exactly-once (только at-least-once)
      • Порядок сообщений не гарантируется
      • Ограниченная пропускная способность (по сравнению с Kafka)

  1.3. NATS — КОРОЛЬ СКОРОСТИ И ПРОСТОТЫ
    NATS — это лёгкий, высокопроизводительный брокер для облачных сред.

    СИЛЬНЫЕ СТОРОНЫ:
      • Минимальная задержка (< 1 мс)
      • Простота настройки (один бинарник)
      • Встроенный RPC (Request-Reply)
      • Лёгкость (маленький footprint)
      • Поддержка MQTT и WebSocket
      • JetStream добавляет хранение и гарантии

    СЛАБЫЕ СТОРОНЫ:
      • Простая маршрутизация (только subject)
      • Нет строгого порядка
      • Нет exactly-once (только at-least-once с JetStream)
      • Меньше экосистемы (по сравнению с Kafka)

  2.  ДЕТАЛЬНОЕ СРАВНЕНИЕ ПО КЛЮЧЕВЫМ КРИТЕРИЯМ
  ┌───────────────────────────┬──────────────────────┬──────────────────────┬──────────────────────┐
  │ Критерий                  │ Kafka                │ RabbitMQ             │ NATS                 │
  ├───────────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┤
  │ Модель                    │ Log                  │ Queue                │ Pub/Sub              │
  ├───────────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┤
  │ Хранение                  │ Да (диск)            │ Нет (временное)      │ Да (JetStream)       │
  ├───────────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┤
  │ Replay                    │ Да                   │ Нет                  │ Да (JetStream)       │
  ├───────────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┤
  │ Порядок                   │ Да (в партиции)      │ Нет                  │ Нет                  │
  ├───────────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┤
  │ Задержка                  │ 5-20 мс              │ < 5 мс               │ < 1 мс               │
  ├───────────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┤
  │ Пропускная способность    │ > 1 млн msg/s        │ ~ 100 тыс msg/s      │ > 10 млн msg/s       │
  ├───────────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┤
  │ Гарантии доставки         │ Exactly-once         │ At-least-once        │ At-least-once        │
  ├───────────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┤
  │ Маршрутизация             │ Простая (по ключу)   │ Сложная (4 exchange) │ Простая (subject)    │
  ├───────────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┤
  │ RPC                       │ Нет                  │ Да                   │ Да (встроенный)      │
  ├───────────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┤
  │ Сложность эксплуатации    │ Высокая              │ Средняя              │ Низкая               │
  ├───────────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┤
  │ Поддержка MQTT            │ Нет                  │ Да (плагин)          │ Да (плагин)          │
  ├───────────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┤
  │ Протокол                  │ Бинарный (свой)      │ AMQP 0-9-1           │ Текстовый (свой)     │
  ├───────────────────────────┼──────────────────────┼──────────────────────┼──────────────────────┤
  │ Экосистема                │ Огромная             │ Хорошая              │ Растущая             │
  └───────────────────────────┴──────────────────────┴──────────────────────┴──────────────────────┘

  3.  СЦЕНАРИИ ИСПОЛЬЗОВАНИЯ КАЖДОГО БРОКЕРА

  3.1. APACHE KAFKA — КОГДА ВЫБИРАТЬ
    + Event Sourcing (хранение всех событий).
    + Долгосрочное хранение данных (retention).
    + Стриминг в реальном времени (Kafka Streams, Flink).
    + Аналитика больших данных (ClickHouse, Druid).
    + Системы с требованием exactly-once (финансы, платёжки).
    + Сложные ETL пайплайны.
    + Микросервисы с event-driven архитектурой.
    + Системы с требованием строгого порядка.

    - Request-Response (RPC) — Kafka не подходит.
    - Задачи (Task Queues) — лучше RabbitMQ.
    - Системы с низкой задержкой (< 5 мс).
    - Простые pub-sub без хранения.

  3.2. RABBITMQ — КОГДА ВЫБИРАТЬ
    + Задачи (Task Queues) — распределение работы между воркерами.
    + RPC (Request-Response) — синхронные вызовы.
    + Сложная маршрутизация (по типам, категориям, шаблонам).
    + Интеграция с устаревшими системами (AMQP, STOMP).
    + Системы с несколькими протоколами (AMQP, MQTT, STOMP).
    + Приложения, где важна низкая задержка.
    + Системы без требований к replay.

    - Event Sourcing (нет replay).
    - Огромные объёмы данных (Kafka лучше).
    - Exactly-once (RabbitMQ не поддерживает).

  3.3. NATS — КОГДА ВЫБИРАТЬ
    + Микросервисная архитектура (обмен событиями).
    + Request-Response (RPC) — встроенная поддержка.
    + IoT-устройства (низкая задержка, лёгкость).
    + Системы с ultra-low latency (< 1 мс).
    + Облачные среды (Kubernetes, multi-cloud).
    + Системы без требований к строгому порядку.
    + Простые pub-sub без сложной маршрутизации.
    + Edge-вычисления.

    - Exactly-once (только at-least-once с JetStream).
    - Строгий порядок сообщений.
    - Сложная маршрутизация (только subject).

  4.  КАК ПРИНИМАТЬ РЕШЕНИЕ: ПОШАГОВЫЙ АЛГОРИТМ

  ШАГ 1: ОПРЕДЕЛИТЕ ТРЕБОВАНИЯ
    • Нужно ли долгосрочное хранение? → Да → Kafka или NATS JetStream.
    • Нужен ли replay? → Да → Kafka или NATS JetStream.
    • Нужен ли строгий порядок? → Да → Kafka (обязательно).
    • Нужна ли exactly-once? → Да → Kafka.
    • Нужна ли сложная маршрутизация? → Да → RabbitMQ.
    • Нужна ли сверхнизкая задержка (< 1 мс)? → Да → NATS.

  ШАГ 2: ОЦЕНИТЕ НАГРУЗКУ
    • Огромные объёмы данных (> 100k msg/s) → Kafka.
    • Средние объёмы (10-100k msg/s) → RabbitMQ или NATS.
    • Маленькие объёмы (< 10k msg/s) → любой.

  ШАГ 3: ОЦЕНИТЕ ОПЕРАЦИОННУЮ СЛОЖНОСТЬ
    • Есть ли команда для поддержки Kafka? → Kafka.
    • Нужен ли простой деплой? → NATS.
    • Нужна ли сложная маршрутизация? → RabbitMQ.

  ШАГ 4: УЧТИТЕ ЭКОСИСТЕМУ
    • Есть ли интеграции с существующими системами?
    • Используется ли Confluent Cloud?
    • Нужно ли MQTT или STOMP?

  5.  АНТИПАТТЕРНЫ И ЧАСТЫЕ ОШИБКИ

    ИСПОЛЬЗОВАНИЕ KAFKA ДЛЯ ЗАДАЧ (TASK QUEUES)

    • Kafka не предназначена для распределения задач между воркерами.
    • Нет built-in acknowledgment для отдельных сообщений.
    • Лучше использовать RabbitMQ или NATS.

    ИСПОЛЬЗОВАНИЕ RABBITMQ ДЛЯ СОБЫТИЙНОГО ХРАНЕНИЯ (EVENT SOURCING)

    • RabbitMQ не хранит сообщения после ack.
    • Нет replay.
    • Лучше использовать Kafka.

    ИСПОЛЬЗОВАНИЕ NATS БЕЗ JETSTREAM ДЛЯ КРИТИЧНЫХ ДАННЫХ

    • Core NATS — at-most-once.
    • Сообщения теряются при сбое.
    • Для критичных данных нужно включать JetStream.

    ВЫБОР БРОКЕРА "ПОТОМУ ЧТО МОДНО"

    • Не выбирайте Kafka только потому, что это "стандарт".
    • Не выбирайте NATS только потому, что он "быстрый".
    • Выбирайте на основе требований.

    НЕПРАВИЛЬНАЯ ОЦЕНКА НАГРУЗКИ

    • Kafka может быть избыточна для 1000 сообщений в день.
    • NATS может не справиться с 1 млн сообщений в секунду.
    • Тестируйте перед продакшеном.

  6.  МИГРАЦИЯ: КОГДА И КАК ПЕРЕХОДИТЬ

  6.1. МИГРАЦИЯ С RABBITMQ НА KAFKA
    • Симптомы: нужен replay, exactly-once, долгосрочное хранение.
    • Стратегия: использовать Kafka как sidecar (читать из RabbitMQ
      и писать в Kafka).
    • Постепенно переводить consumers на Kafka.

  6.2. МИГРАЦИЯ С KAFKA НА NATS
    • Симптомы: нужна низкая задержка, лёгкость, простой деплой.
    • Стратегия: использовать NATS JetStream для критичных данных.
    • Переводить некритичные потоки на NATS.

  6.3. МИГРАЦИЯ С RABBITMQ НА NATS
    • Симптомы: нужна скорость, лёгкость, облачная среда.
    • Стратегия: использовать NATS Core для простых pub-sub,
      NATS JetStream для задач с гарантиями.

  7.  МАТРИЦА РЕШЕНИЙ (QUICK REFERENCE)
  ┌───────────────────────────────────────┬──────────────┬──────────────┬─────────────┐
  │ Сценарий                              │ Kafka        │ RabbitMQ     │ NATS        │
  ├───────────────────────────────────────┼──────────────┼──────────────┼─────────────┤
  │ Event Sourcing / Event Store          │ +            │ -            │ -           │
  ├───────────────────────────────────────┼──────────────┼──────────────┼─────────────┤
  │ Streaming / Analytics                 │ +            │ -            │ -           │
  ├───────────────────────────────────────┼──────────────┼──────────────┼─────────────┤
  │ Task Queues / Workers                 │ -            │ +            │ +           │
  ├───────────────────────────────────────┼──────────────┼──────────────┼─────────────┤
  │ RPC (Request-Response)                │ -            │ +            │ +           │
  ├───────────────────────────────────────┼──────────────┼──────────────┼─────────────┤
  │ Complex Routing                       │ -            │ +            │ -           │
  ├───────────────────────────────────────┼──────────────┼──────────────┼─────────────┤
  │ Microservices (simple pub-sub)        │ +            │ +            │ +           │
  ├───────────────────────────────────────┼──────────────┼──────────────┼─────────────┤
  │ IoT (low resources, low latency)      │ -            │ -            │ +           │
  ├───────────────────────────────────────┼──────────────┼──────────────┼─────────────┤
  │ Exactly-once                          │ +            │ -            │ -           │
  ├───────────────────────────────────────┼──────────────┼──────────────┼─────────────┤
  │ Replay                                │ +            │ -            │ +           │
  ├───────────────────────────────────────┼──────────────┼──────────────┼─────────────┤
  │ Ultra-low latency (< 1 ms)            │ -            │ -            │ +           │
  ├───────────────────────────────────────┼──────────────┼──────────────┼─────────────┤
  │ Cloud-native / Kubernetes             │ -            │ -            │ +           │
  └───────────────────────────────────────┴──────────────┴──────────────┴─────────────┘

  8.  CAP-ТЕОРЕМА И ВЫБОР БРОКЕРА
  CAP-теорема утверждает, что в распределённой системе невозможно одновременно
  обеспечить Consistency (C), Availability (A) и Partition Tolerance (P).
  Вы должны выбрать два из трёх.

  8.1. KAFKA — CP (CONSISTENCY + PARTITION TOLERANCE)
    Kafka жертвует доступностью в пользу консистентности и устойчивости
    к сетевым разделам.

    • При сбое лидера выбор нового лидера происходит только из ISR.
    • Если ISR меньше min.insync.replicas, запись отклоняется.
    • Это даёт строгую консистентность, но может снизить доступность.

  8.2. RABBITMQ — AP (AVAILABILITY + PARTITION TOLERANCE)
    RabbitMQ жертвует консистентностью в пользу доступности.

    • При сетевом разделе кластер может распасться на две части.
    • RabbitMQ продолжает работать на обеих частях (brain split).
    • Это даёт высокую доступность, но возможна потеря консистентности.

  8.3. NATS — AP (AVAILABILITY + PARTITION TOLERANCE)
    NATS также выбирает доступность и устойчивость к сетевым разделам.

    • Core NATS — полностью AP (at-most-once).
    • JetStream добавляет некоторую консистентность через RAFT, но всё ещё
      отдаёт приоритет доступности.

  9.  USE-CASE МАТРИЦА С РЕАЛЬНЫМИ ПРИМЕРАМИ
  ┌───────────────────────────────┬──────────────────────────────────────────┐
  │ Use-case                      │ Реальный пример                          │
  ├───────────────────────────────┼──────────────────────────────────────────┤
  │ Event Sourcing                │ Банковские транзакции (все события       │
  │                               │ должны быть сохранены и воспроизводимы)  │
  ├───────────────────────────────┼──────────────────────────────────────────┤
  │ Streaming Analytics           │ Рекомендательные системы (Netflix,       │
  │                               │ Uber, Spotify)                           │
  ├───────────────────────────────┼──────────────────────────────────────────┤
  │ Task Queues                   │ Обработка заказов, отправка email,       │
  │                               │ генерация отчетов                        │
  ├───────────────────────────────┼──────────────────────────────────────────┤
  │ RPC (Request-Response)        │ API Gateway, микросервисы                │
  ├───────────────────────────────┼──────────────────────────────────────────┤
  │ Microservices (simple pub-sub)│ События пользователей, уведомления       │
  ├───────────────────────────────┼──────────────────────────────────────────┤
  │ IoT                           │ Сенсоры, устройства, telemetry           │
  ├───────────────────────────────┼──────────────────────────────────────────┤
  │ Complex Routing               │ Логирование по уровням, маршрутизация    │
  │                               │ запросов по приоритету                   │
  └───────────────────────────────┴──────────────────────────────────────────┘

  9.1. КОМПАНИИ, ИСПОЛЬЗУЮЩИЕ КАЖДЫЙ БРОКЕР
    KAFKA:
      • Netflix — стриминг данных, analytics.
      • Uber — трекинг поездок, логи.
      • LinkedIn — activity streams.
      • Airbnb — event-driven архитектура.
      • Shopify — обработка заказов.
      • Twitter — real-time analytics.

    RABBITMQ:
      • Reddit — задачи (task queues).
      • GitHub — CI/CD pipelines.
      • Stack Overflow — фоновые задачи.
      • NASA — управление данными.
      • Deutsche Bank — финансовые транзакции.

    NATS:
      • Synadia (разработчики NATS) — сама платформа.
      • VMware — облачные решения.
      • SAP — бизнес-приложения.
      • Apple — внутренние системы.
      • Siemens — IoT и промышленность.

  10.  СТРАТЕГИИ МИГРАЦИИ МЕЖДУ БРОКЕРАМИ

  10.1. МИГРАЦИЯ С RABBITMQ НА KAFKA
    ШАГ 1: Дублирование
      • Настроить Kafka как дополнительный выход для продюсеров.
      • Писать одновременно в RabbitMQ и Kafka.
      • Консюмеры продолжают читать из RabbitMQ.

    ШАГ 2: Переключение консюмеров
      • Постепенно переводить консюмеры на Kafka.
      • Использовать Kafka Consumer Groups.

    ШАГ 3: Отключение RabbitMQ
      • Убедиться, что все консюмеры перешли на Kafka.
      • Отключить продюсеров от RabbitMQ.

  10.2. МИГРАЦИЯ С KAFKA НА NATS
    ШАГ 1: Анализ потребностей
      • Определить, какие потоки нуждаются в replay и exactly-once.
      • Для критичных потоков использовать NATS JetStream.

    ШАГ 2: Постепенный переход
      • Некритичные потоки перевести на NATS Core.
      • Критичные потоки оставить на Kafka.

    ШАГ 3: Оптимизация
      • Настроить NATS для максимальной производительности.

  10.3. МИГРАЦИЯ С RABBITMQ НА NATS
    ШАГ 1: Оценка маршрутизации
      • Проанализировать, нужна ли сложная маршрутизация.
      • Если нет — переходить на NATS.

    ШАГ 2: Поэтапный переход
      • Перевести простые pub-sub потоки на NATS Core.
      • Для задач использовать NATS JetStream.

  11. ГИБРИДНЫЕ АРХИТЕКТУРЫ
  В реальных проектах часто используют комбинацию брокеров.

  11.1. KAFKA + RABBITMQ
    • Kafka для event sourcing и стриминга.
    • RabbitMQ для задач и RPC.

    ПРИМЕР: Платёжная система
      • Kafka хранит все события заказов (event sourcing).
      • RabbitMQ распределяет задачи между воркерами (обработка платежей).

  11.2. KAFKA + NATS
    • Kafka для долгосрочного хранения и replay.
    • NATS для низколатентных микросервисов.

    ПРИМЕР: Финансовая платформа
      • Kafka для аудита и аналитики.
      • NATS для обмена данными между микросервисами в реальном времени.

  11.3. RABBITMQ + NATS
    • RabbitMQ для сложной маршрутизации.
    • NATS для скорости и простоты.

    ПРИМЕР: IoT платформа
      • RabbitMQ для маршрутизации запросов по типам устройств.
      • NATS для передачи данных с устройств.

  12. PERFORMANCE TUNING ДЛЯ КАЖДОГО БРОКЕРА

  12.1. KAFKA
    • batch.size — увеличить для большей пропускной способности.
    • linger.ms — настроить для баланса задержки и пропускной способности.
    • compression.type — использовать snappy или lz4.
    • num.network.threads — увеличить для большей сетевой пропускной
      способности.
    • num.io.threads — увеличить для более быстрой записи.

  12.2. RABBITMQ
    • prefetch_count — настроить для баланса производительности и надёжности.
    • queue durability — использовать только для важных данных.
    • persistent messages — использовать только для важных данных.
    • vm_memory_high_watermark — увеличить для большей памяти.

  12.3. NATS
    • write_deadline — настроить для баланса задержки и надёжности.
    • max_payload — увеличить для больших сообщений.
    • reconnect — настроить для быстрого восстановления.
    • JetStream — использовать FileStorage для постоянного хранения.

  13. ANTI-PATTERNS И ПОЧЕМУ ОНИ ОПАСНЫ

    "KAFKA КАК ОЧЕРЕДЬ ЗАДАЧ"
    Проблема: Kafka не поддерживает индивидуальные подтверждения (ack)
    для каждого сообщения. Если воркер упадёт, сообщение не будет
    переотправлено автоматически.
    Решение: Использовать RabbitMQ или NATS для задач.

    "RABBITMQ ДЛЯ СОБЫТИЙНОГО ХРАНЕНИЯ"
    Проблема: RabbitMQ не хранит сообщения после ack. Нет replay.
    Решение: Использовать Kafka для событийного хранения.

    "NATS CORE ДЛЯ КРИТИЧНЫХ ДАННЫХ"
    Проблема: Core NATS — at-most-once. Сообщения теряются при сбое.
    Решение: Использовать NATS JetStream для критичных данных.

    "ИСПОЛЬЗОВАНИЕ ОДНОГО КАНАЛА ДЛЯ ВСЕГО В RABBITMQ"
    Проблема: Один канал — узкое место. Блокировка при конкурентной записи.
    Решение: Использовать отдельный канал для каждой горутины.

     "БОЛЬШИЕ СООБЩЕНИЯ В KAFKA"
    Проблема: Kafka не предназначена для больших сообщений (> 1 МБ).
    Это приводит к проблемам с памятью и GC.
    Решение: Использовать внешнее хранилище (S3) и ссылки в сообщениях.

  14. FUTURE TRENDS — КУДА ДВИЖЕТСЯ РЫНОК

  14.1. RISE OF MANAGED SERVICES
    • Confluent Cloud, AWS MSK, Azure Event Hubs — растёт популярность.
    • Cloud-native подходы вытесняют self-hosted.

  14.2. JETSTREAM AND KAFKA COMPATIBILITY
    • NATS JetStream становится альтернативой Kafka для small-scale проектов.
    • Поддержка Kafka API в NATS (через плагины).

  14.3. STREAMLINING
    • Упрощение настройки и эксплуатации.
    • Автоматическое масштабирование.

  14.4. AI/ML INTEGRATION
    • Брокеры сообщений интегрируются с ML-пайплайнами.
    • Kafka Streams и Flink для real-time ML.

  14.5. EDGE COMPUTING
    • NATS лидирует в edge-вычислениях.
    • Лёгкость и низкая задержка — ключевые факторы.

  15. КЛЮЧЕВЫЕ ВЫВОДЫ ДЛЯ СОБЕСЕДОВАНИЯ (РАСШИРЕННЫЕ)
  1.  Нет одного "лучшего" брокера. Выбор зависит от CAP-теоремы,
      требований и компромиссов.
  2.  Kafka — CP (Consistency + Partition Tolerance).
      Жертвует доступностью ради консистентности.
  3.  RabbitMQ — AP (Availability + Partition Tolerance).
      Жертвует консистентностью ради доступности.
  4.  NATS — AP (Availability + Partition Tolerance).
      Жертвует консистентностью ради скорости.
  5.  Реальные примеры: Kafka — Netflix, RabbitMQ — GitHub, NATS — VMware.
  6.  Миграция — постепенный процесс. Используйте дублирование и
      поэтапное переключение.
  7.  Гибридные архитектуры — норма. Kafka для стриминга, RabbitMQ для задач,
      NATS для микросервисов.
  8.  Performance tuning — ключевой навык для production.
  9.  Anti-patterns: не используйте Kafka как очередь, RabbitMQ для
      event sourcing, NATS Core для критичных данных.
  10. Future trends: managed services, JetStream, edge computing, AI/ML.
  11. На собеседовании: «Мы выбрали Kafka для стриминга, потому что
      нужен replay и exactly-once. Для задач используем RabbitMQ, а для
      микросервисов — NATS.»
*/
