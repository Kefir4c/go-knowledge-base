package main

/*
  БЛОК 1.2: ТОПИКИ И ПАРТИЦИИ
  Топики и партиции — это основа архитектуры Kafka. Понимание того, как
  они работают на всех уровнях (логическом, физическом, эксплуатационном),
  отличает инженера от администратора.

  СОДЕРЖАНИЕ:
    1. Топики — логическая категория
    2. Партиции — физическая единица
    3. Внутреннее устройство партиции (сегменты, индексы)
    4. Партиционирование: стратегии и ключи
    5. Выбор количества партиций
    6. Добавление партиций — последствия и процедура
    7. Горячие партиции (Hot Partitions)
    8. Распределение партиций по брокерам
    9. Мониторинг партиций
    10. Ключевые выводы для собеседования

  1.  ТОПИКИ — ЛОГИЧЕСКАЯ КАТЕГОРИЯ
  Топик — это именованная категория данных. Он служит для группировки
  сообщений по бизнес-смыслу (например, «заказы», «платежи», «логи»).

  ОСОБЕННОСТИ ТОПИКОВ:
    • Топик не имеет фиксированного размера — он растёт с течением времени,
      пока не истечёт время хранения (retention).
    • Топики могут иметь множество подписчиков (консюмеров).
    • Топики конфигурируются отдельно: количество партиций, фактор
      репликации, политика очистки.

  2.  ПАРТИЦИИ — ФИЗИЧЕСКАЯ ЕДИНИЦА
  Партиция — это физическое разделение топика. Каждая партиция представляет
  собой упорядоченный, неизменяемый лог (append-only log).

  КЛЮЧЕВЫЕ ПРИНЦИПЫ:
    • ПОРЯДОК: сообщения в партиции строго упорядочены по offset.
      Если вам нужна гарантия порядка — все сообщения должны попадать
      в одну партицию.
    • ПАРАЛЛЕЛИЗМ: одна партиция обрабатывается только одним консюмером
      в группе. Количество партиций определяет максимальный параллелизм.
    • НЕИЗМЕНЯЕМОСТЬ: сообщения никогда не изменяются. Удаление происходит
      только при очистке сегментов по retention.

  3.  ВНУТРЕННЕЕ УСТРОЙСТВО ПАРТИЦИИ (СЕГМЕНТЫ, ИНДЕКСЫ)
  Каждая партиция физически представлена директорией с набором сегментов.

  3.1. СЕГМЕНТЫ (LOG SEGMENTS)
  Партиция разбивается на сегменты — файлы ограниченного размера
  (по умолчанию 1 ГБ). Когда активный сегмент достигает лимита,
  создаётся новый.

  Файлы сегмента:
    • .log — сами сообщения.
    • .index — разреженный индекс для поиска по offset (offset → position).
    • .timeindex — разреженный индекс для поиска по времени
      (timestamp → offset).

  Имя файла — это baseOffset (смещение первого сообщения в сегменте).

  3.2. ПОИСК СООБЩЕНИЯ ПО OFFSET
  1. Консюмер запрашивает сообщение с offset = X.
  2. Kafka определяет, в каком сегменте находится это смещение
     (по baseOffset).
  3. В .index ищется ближайшая запись с offset ≤ X.
  4. Получается позиция в .log.
  5. Выполняется последовательное чтение из .log до нужного offset.

  Это даёт O(log N) сложность поиска при минимальном размере индекса.

  3.3. РАЗРЕЖЕННЫЙ ИНДЕКС (SPARSE INDEX)
  Индекс не содержит запись для каждого сообщения, а только для каждого
  N-го сообщения (по умолчанию каждые 4 КБ данных в сегменте).
  Это позволяет держать индекс в памяти, экономя ресурсы.

  4.  ПАРТИЦИОНИРОВАНИЕ: СТРАТЕГИИ И КЛЮЧИ

  4.1. ПАРТИЦИОНИРОВАНИЕ ПО КЛЮЧУ (KEY)
  Когда продюсер отправляет сообщение с ключом (key), Kafka вычисляет
  хэш от ключа и определяет партицию:

    partition = hash(key) % number_of_partitions

  ЭТО ДАЁТ:
    • Все сообщения с одинаковым ключом попадают в одну партицию.
    • Сохраняется порядок сообщений для одного ключа.
    • Это гарантирует, что события одного пользователя или заказа
      будут обрабатываться по порядку.

  ВАЖНО: если вы измените количество партиций, хэш-функция изменится,
  и сообщения с одним ключом могут попасть в разные партиции,
  нарушая порядок.

  4.2. ПАРТИЦИОНИРОВАНИЕ БЕЗ КЛЮЧА (NULL KEY)
  Если ключ не указан (null), Kafka использует:
    • Round-robin (по кругу) для равномерного распределения.
    • Или случайное распределение (зависит от версии).

  ЭТО ДАЁТ:
    • Равномерную загрузку партиций.
    • Нет гарантии порядка.

  4.3. КАСТОМНЫЙ ПАРТИЦИОНЕР
  Можно реализовать свой алгоритм распределения:
    • По бизнес-правилам (например, по географическому региону).
    • По временному интервалу.
    • По сложной логике (например, по дате + ID пользователя).

  5.  ВЫБОР КОЛИЧЕСТВА ПАРТИЦИЙ
  Это одно из самых важных архитектурных решений.

  5.1. ФАКТОРЫ, ВЛИЯЮЩИЕ НА ВЫБОР
    • Целевая пропускная способность (сообщений/сек, байт/сек).
    • Количество параллельных консюмеров.
    • Прогнозируемый рост нагрузки.
    • Ресурсы брокеров (память, файловые дескрипторы).

  5.2. ФОРМУЛА ПРИБЛИЖЁННОГО РАСЧЁТА
    partitions = max(
      producer_throughput_mbps / single_partition_throughput_mbps,
      consumer_throughput_mbps / single_partition_throughput_mbps
    )

    где single_partition_throughput_mbps ≈ 10-50 МБ/с (зависит от железа).

  5.3. ПРАКТИЧЕСКИЕ РЕКОМЕНДАЦИИ
    • Начинайте с 3-6 партиций для небольших проектов.
    • Для средних проектов — 10-20 партиций.
    • Для крупных — 50-100+ партиций.
    • Умножайте на 2-3 для запаса роста.
    • Лучше иметь больше партиций, чем меньше — их можно добавить,
      но нельзя удалить.

  6.  ДОБАВЛЕНИЕ ПАРТИЦИЙ — ПОСЛЕДСТВИЯ И ПРОЦЕДУРА

  6.1. КАК ЭТО РАБОТАЕТ
  При добавлении партиций:
    • Изменяется хэш-функция для определения партиции.
    • Существующие данные остаются на месте (не перераспределяются).
    • Новые сообщения будут распределяться по новому алгоритму.

  6.2. ВЛИЯНИЕ НА ПОРЯДОК
  Если вы используете партиционирование по ключу, добавление партиций
  может нарушить порядок для существующих ключей, потому что хэш
  теперь будет давать другое значение.

  ПРИМЕР: было 3 партиции, key="user123" попадал в партицию 1.
  Стало 5 партиций, тот же ключ может попасть в партицию 3.
  Сообщения до и после добавления окажутся в разных партициях.

  РЕШЕНИЕ:
    • Добавляйте партиции ДО того, как ключи начнут активно использоваться,
      если это возможно.
    • Или используйте кастомный партиционер, который игнорирует количество
      партиций (например, партиционирование по диапазону).

  6.3. ПРОЦЕДУРА ДОБАВЛЕНИЯ
    kafka-topics.sh --alter --topic my-topic --partitions 10

  ПАРТИЦИИ НЕЛЬЗЯ УДАЛИТЬ. Единственный способ «удалить» — создать
  новый топик с нужным количеством партиций и перенести данные.

  7.  ГОРЯЧИЕ ПАРТИЦИИ (HOT PARTITIONS)

  7.1. ОПРЕДЕЛЕНИЕ
  Горячая партиция — это партиция, которая получает непропорционально
  большое количество сообщений по сравнению с другими.

  7.2. ПРИЧИНЫ
    • Неравномерное распределение ключей (один популярный ключ).
    • Ошибка в кастомном партиционере.
    • Неправильное количество партиций.

  7.3. ПОСЛЕДСТВИЯ
    • Перегрузка одного брокера.
    • Увеличение задержки для сообщений в этой партиции.
    • Снижение общей производительности кластера.

  7.4. РЕШЕНИЯ
    • Использовать составные ключи: key = user_id + timestamp,
      чтобы разбить популярный ключ.
    • Добавить «соль» (salt) к ключу: key = user_id + salt,
      где salt меняется случайно.
    • Использовать кастомный партиционер с балансировкой по нагрузке.
    • Увеличить количество партиций, чтобы размазать нагрузку.

  8.  РАСПРЕДЕЛЕНИЕ ПАРТИЦИЙ ПО БРОКЕРАМ

  8.1. АВТОМАТИЧЕСКОЕ РАСПРЕДЕЛЕНИЕ
  При создании топика Kafka автоматически распределяет партиции по брокерам
  по принципу round-robin.

  ПРИМЕР (3 брокера, 6 партиций, replication-factor=2):
    Партиция 0: leader=broker0, replica=broker1
    Партиция 1: leader=broker1, replica=broker2
    Партиция 2: leader=broker2, replica=broker0
    Партиция 3: leader=broker0, replica=broker2
    Партиция 4: leader=broker1, replica=broker0
    Партиция 5: leader=broker2, replica=broker1

  8.2. ПЕРЕРАСПРЕДЕЛЕНИЕ (REASSIGNMENT)
  При добавлении новых брокеров партиции автоматически не перераспределяются.
  Для этого используется:
    • kafka-reassign-partitions.sh — ручное перераспределение.
    • Управляемые сервисы (Amazon MSK, Confluent) делают это автоматически.

  8.3. БАЛАНСИРОВКА НАГРУЗКИ
  В идеале каждый брокер должен нести примерно одинаковую нагрузку
  (количество партиций, размер данных, пропускная способность).
  Мониторинг помогает выявить дисбаланс.

  9.  МОНИТОРИНГ ПАРТИЦИЙ

  9.1. КЛЮЧЕВЫЕ МЕТРИКИ
    • Размер партиции (в байтах и сообщениях).
    • Пропускная способность (сообщений/сек, байт/сек).
    • Задержка консюмера (consumer lag) — разница между последним offset
      в партиции и текущим offset консюмера.
    • ISR shrink/expand — изменение количества реплик в ISR.

  9.2. ИНСТРУМЕНТЫ
    • Kafka Manager (CMAK) — визуальное управление.
    • Confluent Control Center — платный инструмент.
    • Prometheus + JMX exporter — для сбора метрик.
    • kafka-consumer-groups.sh — CLI для проверки lag.

  9.3. ПРЕДУПРЕЖДАЮЩИЕ СИГНАЛЫ
    • Резкий рост размера одной партиции (hot partition).
    • Рост consumer lag (консюмер не успевает).
    • Уменьшение количества реплик в ISR (проблемы с репликацией).

  10. КЛЮЧЕВЫЕ ВЫВОДЫ ДЛЯ СОБЕСЕДОВАНИЯ
  1. Топик — логическая категория. Партиция — физическая единица хранения.
  2. Порядок сообщений гарантируется ТОЛЬКО внутри одной партиции.
  3. Партиция — единица параллелизма: одна партиция → один консюмер.
  4. При выборе количества партиций учитывайте пропускную способность
     и рост нагрузки. Лучше иметь запас.
  5. Партиционирование по ключу обеспечивает порядок для одного ключа.
     Без ключа — round-robin.
  6. Добавление партиций меняет хэш-функцию и может нарушить порядок
     для существующих ключей.
  7. Партиции НЕЛЬЗЯ УДАЛИТЬ — только создать новый топик.
  8. Горячие партиции — частая проблема в продакшене.
     Решение: составные ключи, соль, кастомный партиционер.
  9. Мониторинг партиций (размер, lag, ISR) обязателен.
  10. Партиции распределяются по брокерам равномерно, но при добавлении
      брокеров требуется перераспределение.
*/
