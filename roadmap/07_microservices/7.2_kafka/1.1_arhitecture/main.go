package main

/*
БЛОК 1.1: АРХИТЕКТУРА KAFKA
Apache Kafka — это распределённая платформа для стриминга, которая сочетает
в себе возможности очереди сообщений, системы хранения и обработки потоков
данных в реальном времени. Она спроектирована как распределённый,
партиционированный и реплицируемый commit log. Простыми словами: это
механизм, который позволяет миллионам продюсеров отправлять триллионы
сообщений в день, а тысячам консюмеров читать их с гарантиями порядка
и сохранности.

Kafka изначально создавалась в LinkedIn, затем стала open-source проектом
Apache. Сегодня она используется в 80% Fortune 500 компаний и является
стандартом де-факто для стриминговых архитектур.

1. КЛЮЧЕВЫЕ КОМПОНЕНТЫ АРХИТЕКТУРЫ
Любая система Kafka строится вокруг нескольких базовых абстракций.
Их нужно понимать наизусть, как таблицу умножения.

1.1. БРОКЕР (Broker)
Брокер — это сервер в кластере Kafka. Кластер может состоять из одного
или нескольких брокеров. Каждый брокер хранит данные и обрабатывает
запросы от продюсеров и консюмеров. Брокеры обмениваются данными через
высокопроизводительный TCP-протокол.

Брокеры выполняют следующие функции:
• Хранят партиции топиков на диске.
• Принимают запросы на запись от продюсеров.
• Отвечают на запросы на чтение от консюмеров.
• Участвуют в репликации данных между собой.
• Выполняют выборы лидера партиции при сбоях.
• Координируют работу consumer groups.

Ключевая особенность Kafka — брокеры stateless по отношению к данным:
все смещения и метаданные хранятся отдельно (в ZooKeeper/KRaft и в топике
__consumer_offsets).

1.2. ТОПИК (Topic)
Топик — это логическая категория, в которую публикуются сообщения.
Топик — это именованный поток записей. Аналог таблицы в БД или очереди
в RabbitMQ.

Топики всегда поддерживают нескольких подписчиков — один топик может
иметь сколько угодно консюмеров, читающих данные в своём темпе.

Конфигурация топика включает:
• Количество партиций.
• Коэффициент репликации (обычно 2-3).
• Время хранения данных (log.retention.hours/ms).
• Политику очистки (delete или compact).
• Размер сегментов.
• Настройки сжатия.

Топик может быть создан автоматически при первой отправке сообщения
(auto.create.topics.enable=true) или вручную через Admin API.

1.3. ПАРТИЦИЯ (Partition)
Партиция — это физическое разделение топика. Каждый топик состоит из одной
или нескольких партиций. Каждая партиция представляет собой упорядоченную,
неизменяемую последовательность записей, в которую постоянно добавляются
новые сообщения (append-only log).

Каждая партиция — это отдельный файл на диске. Это позволяет распределять
данные по разным брокерам и обеспечивать параллельную обработку.

Сообщения внутри партиции упорядочены, и порядок гарантируется.
Порядок между разными партициями не гарантируется.

Количество партиций в топике определяет уровень параллелизма:
• Больше партиций — больше параллелизма.
• Больше партиций — больше накладных расходов (файловые дескрипторы,
  память на индексы, сетевой трафик).
• Максимальное рекомендуемое количество партиций для одного брокера —
  2000-4000 в зависимости от оборудования.

1.4. СМЕЩЕНИЕ (Offset)
Offset — это уникальный последовательный идентификатор записи внутри
партиции. Это 64-битное целое число, которое монотонно возрастает.

Offset логически уникален внутри партиции, но не уникален в масштабах
всего кластера. Например, в партиции 0 offset 5 — это одно сообщение,
в партиции 1 offset 5 — другое.

Offset присваивается автоматически при записи. Консюмеры используют
offset для отслеживания своей позиции в логе.

Offset может быть reset (сброшен) через auto.offset.reset.

1.5. ПРОДЮСЕР (Producer)
Продюсер — это приложение, которое публикует поток записей в один или
несколько топиков. Продюсер сам выбирает партицию, в которую отправить
сообщение:

• Если указан ключ (key), хэш ключа определяет номер партиции.
• Если ключ не указан, используется round-robin или sticky-партиционер.
• Можно также указать партицию явно через custom partitioner.

Продюсер может работать в синхронном или асинхронном режиме.
Он может отправлять батчи сообщений для повышения производительности.

Основные настройки продюсера:
• bootstrap.servers — список брокеров для подключения.
• key.serializer / value.serializer — сериализаторы.
• acks — уровень подтверждения (0, 1, all).
• retries — количество повторных попыток.
• batch.size — размер батча.
• linger.ms — время ожидания накопления батча.
• compression.type — тип сжатия (gzip, snappy, lz4, zstd).

1.6. КОНСЮМЕР (Consumer)
Консюмер — это приложение, которое подписывается на один или несколько
топиков и обрабатывает поступающие записи.

Консюмеры объединяются в consumer groups. В рамках одной группы каждая
партиция может читаться только одним консюмером.

Консюмер может читать как старые данные (earliest), так и новые (latest).

Основные настройки консюмера:
• bootstrap.servers — список брокеров.
• key.deserializer / value.deserializer — десериализаторы.
• group.id — идентификатор группы.
• auto.offset.reset — стратегия при отсутствии смещения.
• enable.auto.commit — автоматический или ручной коммит.
• max.poll.records — максимальное количество записей за один poll.
• session.timeout.ms — таймаут сессии консюмера.

1.7. КЛАСТЕР (Cluster)

Кластер — это группа брокеров, работающих вместе. Kafka всегда запускается
как кластер, даже если в нём один брокер.

Кластер обеспечивает:
• Отказоустойчивость — при падении одного брокера данные не теряются.
• Масштабируемость — можно добавлять брокеры горизонтально.
• Репликацию — данные копируются между брокерами.

2. ХРАНЕНИЕ ДАННЫХ НА ДИСКЕ
Это одна из самых важных тем — понимание того, как Kafka достигает
своей феноменальной производительности. Без этого ты не поймёшь,
почему Kafka такая быстрая и как её правильно настраивать.

2.1. СЕГМЕНТЫ (LOG SEGMENTS)
Каждая партиция физически представлена в виде директории на диске.
Внутри директории данные разбиты на сегменты (Log Segments).

Структура директории партиции (пример для топика "topic-log", партиция 0):

topic-log-0/
  ├── 00000000000000000000.log      # данные
  ├── 00000000000000000000.index    # смещения → позиции
  ├── 00000000000000000000.timeindex # временные метки → смещения
  ├── 00000000000000000133.log
  ├── 00000000000000000133.index
  └── 00000000000000000133.timeindex

Каждый сегмент состоит из трёх основных файлов:
• .log — содержит сами сообщения (данные). Это основной файл.
• .index — разреженный индекс для поиска по offset (смещение → позиция в .log).
• .timeindex — разреженный индекс для поиска по времени (timestamp → offset).

Имена файлов — это baseOffset (смещение первого сообщения в сегменте),
дополненное нулями до 20 цифр. Например, 00000000000000000000.log означает,
что первый offset в этом сегменте — 0.

Активный сегмент (active segment) — это последний сегмент, в который
в данный момент записываются сообщения. Только он доступен для записи.
Все предыдущие сегменты доступны только для чтения.

Когда активный сегмент достигает определённого размера (log.segment.bytes)
или возраста (log.roll.hours), он закрывается и создаётся новый.

2.2. SEQUENTIAL WRITE — ГЛАВНЫЙ СЕКРЕТ ПРОИЗВОДИТЕЛЬНОСТИ
Kafka записывает все сообщения строго последовательно в конец файла
(append-only). Это принципиальное отличие от многих других систем,
включая традиционные БД.

ПОЧЕМУ ЭТО БЫСТРО:

• Последовательная запись на диск (sequential I/O) в сотни и тысячи раз
  быстрее случайной записи (random I/O).
• На HDD: случайная запись — сотни KB/s, последовательная — сотни MB/s.
• Головка жёсткого диска не перемещается случайным образом, что устраняет
  главное узкое место.
• На SSD: последовательная запись также быстрее случайной, хотя разница
  не такая драматичная (но всё равно значительная).

КАК ЭТО РАБОТАЕТ В KAFKA:
• Все сообщения записываются только в конец активного .log-файла.
• Уже записанные сообщения никогда не изменяются и не удаляются из файла.
• Каждая партиция — это отдельный файл, и запись в разные партиции
  может выполняться параллельно.

ЭТО ДАЁТ:
• Огромную пропускную способность (сотни MB/s на один брокер).
• Малую задержку (миллисекунды).
• Возможность использования HDD вместо дорогих SSD для хранения больших
  объёмов данных.

2.3. PAGE CACHE И ZERO COPY
Kafka использует Page Cache операционной системы для кэширования данных.
• Сообщения записываются сначала в Page Cache (в памяти), а затем
  асинхронно сбрасываются на диск.
• При чтении данные также читаются из Page Cache, если они там есть.
• Это обеспечивает очень высокую производительность для горячих данных.

Zero-Copy — технология, позволяющая передавать данные из файла напрямую
в сетевой сокет, минуя пользовательское пространство.

Обычный путь:
  диск → Page Cache → память приложения → сетевой буфер

Zero-Copy:
  диск → Page Cache → сетевой буфер (минуя память приложения)

Это снижает накладные расходы на копирование данных и увеличивает
пропускную способность при чтении.

2.4. РАЗРЕЖЕННЫЕ ИНДЕКСЫ (SPARSE INDEXES)
Kafka не индексирует каждое сообщение — это было бы слишком дорого.
Вместо этого она использует разреженные индексы.

• .INDEX (смещение → позиция в .log):
  - Хранит не все смещения, а только некоторые (каждые N байт или
    каждые M сообщений, настраивается через index.interval.bytes).
  - Позволяет быстро найти примерную позицию сообщения в .log-файле.
  - Далее выполняется небольшой последовательный перебор в .log.

• .TIMEINDEX (временная метка → смещение):
  - Позволяет искать сообщения по времени.
  - Сначала находится смещение по времени, затем по смещению — позиция в .log.
  - Используется в команде kafka-consumer-groups для поиска по времени.

• ИНДЕКСЫ В ПАМЯТИ (MMap):
  - Индексы загружаются в память через Memory-Mapped Files (MMap).
  - Это позволяет выполнять поиск по индексам без обращения к диску.
  - Индексы не загружаются в JVM-кучу, что экономит память и снижает
    нагрузку на GC.

Размер индекса можно рассчитать: примерно 4-8 байт на сообщение.
При 1 миллиарде сообщений индекс будет занимать около 4-8 ГБ.

2.5. LOG COMPACTION (СЖАТИЕ ЛОГА)
Kafka поддерживает два режима очистки данных:
• Удаление по времени/размеру (log.cleanup.policy=delete).
• Сжатие (log.cleanup.policy=compact).

При сжатии (Log Compaction) сохраняется только последнее сообщение для
каждого ключа. Это полезно для таблиц состояний, хранения последних
значений (например, цена акции, баланс пользователя, статус заказа).

Механизм compaction работает в фоновом режиме: периодически создаются
урезанные копии сегментов, в которых остаются только последние версии
сообщений по каждому ключу.

Когда использовать log compaction:
• Хранение последнего состояния.
• Репликация таблиц между системами.
• Change Data Capture (CDC).

2.6. FLUSH НА ДИСК
Kafka не сбрасывает данные на диск сразу после каждой записи.
Данные сначала записываются в Page Cache, а затем асинхронно сбрасываются
на диск.

Это позволяет достичь высокой пропускной способности, но создаёт риск
потери данных при сбое питания. Для минимизации этого риска используются:
• fsync() — синхронная запись на диск (замедляет производительность).
• Настройка flush.messages / flush.ms (число сообщений или время).

По умолчанию Kafka полагается на OS для сброса данных на диск.
В большинстве случаев этого достаточно.

3. РЕПЛИКАЦИЯ И НАДЁЖНОСТЬ
Это критически важная тема для продакшена. Kafka — это распределённая
система, и она должна быть надёжной.

3.1. ISR (IN-SYNC REPLICAS)
Каждая партиция имеет одного лидера и несколько реплик (followers).
Лидер обрабатывает все запросы на запись и большинство запросов на чтение.

Реплики бывают двух типов:
• In-Sync Replicas (ISR) — реплики, которые синхронизированы с лидером.
  Они получают все сообщения, которые получил лидер.
• Out-of-Sync Replicas — реплики, отставшие от лидера.

Реплика считается out-of-sync, если она не получала сообщения от лидера
в течение replica.lag.time.max.ms или lag больше replica.lag.max.messages.

Только реплики из ISR могут стать лидером при сбое текущего лидера.
Это гарантирует, что данные не будут потеряны.

3.2. min.insync.replicas
min.insync.replicas — минимальное количество реплик в ISR, которые должны
подтвердить запись, чтобы она считалась успешной.

Пример:
• topic с replication.factor=3 и min.insync.replicas=2.
• При записи с acks=all продюсер ждёт подтверждения от 2 реплик в ISR.
• Если в ISR осталось менее 2 реплик, запись будет отклонена.
• Это гарантирует, что данные не будут потеряны при сбое одного брокера.

Сочетание min.insync.replicas и acks=all даёт гарантию, что данные будут
сохранены даже при сбое брокера.

3.3. acks — НАСТРОЙКА ПРОДЮСЕРА
acks определяет уровень подтверждения для продюсера.

• acks=0:
  - Продюсер не ждёт подтверждения от брокера.
  - Максимальная скорость, минимальная надёжность.
  - Сообщение может быть потеряно при сбое брокера.
  - Используется для систем, где допустима потеря данных
    (логи, метрики, телеметрия).

• acks=1:
  - Продюсер ждёт подтверждения от лидера.
  - Лидер записал сообщение в свой лог, но реплики ещё не получили его.
  - Хороший баланс между скоростью и надёжностью.
  - Сообщение может быть потеряно при сбое лидера до репликации.

• acks=all (-1):
  - Продюсер ждёт подтверждения от всех реплик в ISR.
  - Максимальная надёжность, минимальная скорость.
  - Требует min.insync.replicas > 1.
  - Рекомендуется для критических данных.

3.4. REPLICATION.FACTOR
Количество реплик для каждого топика. Обычно 2 или 3.

• replication.factor=1 — минимальная надёжность (потеря данных при сбое).
• replication.factor=2 — один брокер может выйти из строя.
• replication.factor=3 — два брокера могут выйти из строя
  (но не одновременно).

Рекомендуется replication.factor=3 в продакшене.

4. ZOOKEEPER И KRAFT — УПРАВЛЕНИЕ КЛАСТЕРОМ
Это тема, которая принципиально меняет понимание Kafka.
До версии 3.3 Kafka использовала ZooKeeper. Сейчас — KRaft.

4.1. ZOOKEEPER — СТАРАЯ ЭРА
ZooKeeper был обязательным компонентом для работы Kafka.

ЧТО ДЕЛАЛ ZOOKEEPER:
• Хранил метаданные кластера: список брокеров, конфигурации топиков,
  ACL, информацию о партициях и репликах (ISR).
• Выполнял выборы контроллера (Controller) — брокера, который управляет
  кластером.
• Отслеживал состояние брокеров (кто жив, кто умер).
• Хранил смещения консюмеров (до появления внутреннего топика
  __consumer_offsets).

НЕДОСТАТКИ ZOOKEEPER:
• СЛОЖНОСТЬ ОПЕРАЦИЙ: нужно поддерживать две распределённые системы.
• ПРОБЛЕМЫ С СОГЛАСОВАННОСТЬЮ: метаданные хранятся в ZooKeeper,
  контроллер синхронизирует их в своей памяти, а затем рассылает брокерам.
  При сбоях возможны расхождения.
• МЕДЛЕННОЕ ВОССТАНОВЛЕНИЕ: при падении контроллера восстановление
  зависело от сессий ZooKeeper.
• ОГРАНИЧЕНИЯ ПО МАСШТАБУ: при большом количестве партиций ZooKeeper
  становился узким местом.

4.2. KRAFT — НОВАЯ ЭРА
KRaft (Kafka Raft) — это режим работы Kafka, в котором управление кластером
встроено непосредственно в Kafka, а зависимость от ZooKeeper полностью
устранена.

КЛЮЧЕВЫЕ ИЗМЕНЕНИЯ:
• НЕТ ВНЕШНЕЙ ЗАВИСИМОСТИ: для работы кластера больше не нужен
  отдельный ZooKeeper.
• УПРАВЛЕНИЕ ЧЕРЕЗ RAFT: метаданные хранятся в самом Kafka с использованием
  протокола Raft (консенсус).
• КОНТРОЛЛЕРЫ: в KRaft-режиме выделяются специальные серверы — контроллеры,
  которые управляют метаданными.
• ПЕРЕНОС МЕТАДАННЫХ В ТОПИК: метаданные кластера хранятся во внутреннем
  топике __cluster_metadata.

РОЛИ В KRAFT:
• BROKER — обрабатывает запросы на чтение/запись данных.
• CONTROLLER — управляет метаданными кластера.
• COMBINED (broker,controller) — сервер выполняет обе роли (для небольших
  кластеров и разработки).

ПРЕИМУЩЕСТВА KRAFT:
• УПРОЩЕНИЕ ОПЕРАЦИЙ: одна система вместо двух.
• БОЛЕЕ БЫСТРОЕ ВОССТАНОВЛЕНИЕ: Raft обеспечивает более предсказуемое
  и быстрое восстановление после сбоев.
• ЛУЧШАЯ МАСШТАБИРУЕМОСТЬ: сняты ограничения ZooKeeper.
• УЛУЧШЕННАЯ СОГЛАСОВАННОСТЬ: Raft гарантирует строгую согласованность
  метаданных.

КОГДА ПОЯВИЛСЯ KRAFT:
• Kafka 3.3 (2022) — KRaft стал production-ready.
• Kafka 4.0 — ZooKeeper полностью удалён.

5. КОНСЮМЕР-ГРУППЫ И РЕБАЛАНСИРОВКА
Это одна из самых сложных тем, но без её понимания невозможно настроить
Kafka в продакшене.

5.1. КОНСЮМЕР-ГРУППА (CONSUMER GROUP)
Консюмер-группа — это набор консюмеров, которые совместно читают один или
несколько топиков.

ПРАВИЛО:
• В рамках одной группы каждая партиция может читаться только одним
  консюмером.
• Если консюмеров меньше, чем партиций, некоторые консюмеры будут
  читать несколько партиций.
• Если консюмеров больше, чем партиций, лишние консюмеры будут
  простаивать.

5.2. РЕБАЛАНСИРОВКА (REBALANCE)
Rebalance — это процесс перераспределения партиций между консюмерами
в группе. Он происходит при:
• Добавлении нового консюмера в группу.
• Удалении консюмера из группы (выход или сбой).
• Изменении подписки на топики.
• Изменении количества партиций в топике.

Во время ребалансировки потребление останавливается до тех пор, пока
все партиции не будут назначены.

СТРАТЕГИИ РЕБАЛАНСИРОВКИ:
• Range — партиции делятся на диапазоны между консюмерами.
  Пример: 4 партиции, 2 консюмера → консюмер 1 получает 0,1; консюмер 2
  получает 2,3.
  Простая, но не всегда оптимальная.

• RoundRobin — партиции распределяются по кругу.
  Пример: 4 партиции, 2 консюмера → консюмер 1 получает 0,2; консюмер 2
  получает 1,3.
  Более сбалансированная.

• Sticky — минимизирует перемещение партиций при ребалансировке.
  Сохраняет уже существующие назначения.

• Cooperative — инкрементальная ребалансировка.
  Позволяет переназначать только те партиции, которые реально меняют
  владельца, не останавливая работу остальной группы.
  Это современный стандарт.

5.3. STATIC MEMBERSHIP
Статический membership (group.instance.id) позволяет консюмерам
сохранять свои ID при перезапуске. Это уменьшает количество ребалансировок,
потому что Kafka "знает", что консюмер вернётся.

6. КЛЮЧЕВЫЕ ВЫВОДЫ
1. Kafka — распределённый commit log, а не очередь.
2. Сообщения хранятся на диске, не удаляются после чтения.
3. Порядок сообщений гарантируется только внутри партиции.
4. Партиции — единица параллелизма и масштабирования.
5. Sequential write — главный источник производительности.
6. Разреженные индексы, Page Cache и Zero-Copy делают чтение быстрым.
7. ZooKeeper устарел, KRaft — будущее.
8. ISR определяют, какие реплики синхронизированы.
9. min.insync.replicas + acks=all дают надёжность.
10. Rebalance — сложный процесс, требующий понимания.
11. Cooperative Rebalance — современный стандарт.
12. Consumer Lag — ключевая метрика для мониторинга.
13. Kafka позволяет масштабироваться до миллионов событий в секунду.
14. Правильная настройка партиций и репликации критична для продакшена.
15. Консюмер должен быть идемпотентным при at-least-once обработке.
*/
