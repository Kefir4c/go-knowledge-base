package cqrses

/*
  УРОК 7.4: CQRS И EVENT SOURCING
  Это одна из самых сложных тем в архитектуре микросервисов.
  Не потому что код сложный, а потому что она заставляет
  переосмыслить само понятие "данные". Здесь нет "ещё одного
  паттерна" — здесь смена парадигмы.

  СОДЕРЖАНИЕ:
    1.  Боль, которую решают CQRS и ES
    2.  CQRS — разделение путей
    3.  Event Sourcing — состояние как история
    4.  Как они сочетаются — полная картина
    5.  Проекции и Read Models — самая сложная часть
    6.  Консистентность, конкурентность, идемпотентность
    7.  Эволюция событий — версионирование и upcasting
    8.  Event Store — требования и выбор
    9.  Масштабирование — hot aggregates, шардирование, lag
    10. GDPR и право на забвение
    11. Process Managers — саги на событиях
    12. Миграция с CRUD на ES
    13. Когда использовать и когда НЕ использовать
    14. Антипаттерны
    15. Тестирование, мониторинг, трассировка
    16. Финальные выводы

  1. БОЛЬ, КОТОРУЮ РЕШАЮТ CQRS И ES
  Начнём не с паттернов, а с боли. Без боли паттерны — это
  карго-культ. Люди копируют Event Sourcing, не понимая, зачем
  он нужен, и через год проклинают день, когда о нём услышали.

  Боль №1: ИСТОРИЯ ТЕРЯЕТСЯ.
  Представь банковский счёт. Алиса переводит 100 рублей Бобу.
  В CRUD-мире это выглядит так:

    UPDATE accounts SET balance = balance - 100 WHERE user = 'Alice';
    UPDATE accounts SET balance = balance + 100 WHERE user = 'Bob';

  Старое значение 500 просто исчезает. Оно перезаписано на 400.
  Через год никто не скажет, почему баланс именно такой. Никто
  не восстановит состояние на прошлый вторник. Никто не докажет
  аудитору, что операция вообще была.

  Можно, конечно, навесить таблицу audit_log. Но это bolt-on
  решение, у которого три фатальных изъяна:

    - Текущее состояние и аудит живут раздельно. Рано или поздно
      они разойдутся. Это не "если", это "когда".
    - Разработчик забыл записать в аудит — данные потеряны
      навсегда. Аудит — это не обязанность, это дополнительная
      работа, которую всегда хочется пропустить.
    - Триггеры БД пишут "что" изменилось, но не "почему".
      У них нет бизнес-контекста. Они не знают, что перевод
      был компенсацией за брак, или платежом по договору,
      или подарком на день рождения.

  Боль №2: КОНКУРЕНТНОСТЬ.
  Когда много запросов пишут в одну строку, БД начинает её
  лочить. Под нагрузкой это становится узким местом. Индексы
  не помогают — это природа UPDATE. PostgreSQL блокирует
  строку до конца транзакции, и все остальные писатели ждут.

  Боль №3: РАЗНЫЕ СЦЕНАРИИ ЧТЕНИЯ ТРЕБУЮТ РАЗНЫХ МОДЕЛЕЙ.
  Одна нормализованная схема не может быть оптимальной
  одновременно для записи транзакции и для страницы "мои заказы
  за год с фильтрами по статусу и сумме". Ты либо нормализуешь
  под запись и делаешь тяжёлые JOIN'ы на чтении, либо
  денормализуешь под чтение и мучаешься с консистентностью
  при записи.

  Боль №4: АУДИТ И COMPLIANCE.
  Финансы, медицина, юриспруденция. Везде, где нужно доказать
  регулятору: кто, что, когда и почему сделал. CRUD с bolt-on
  аудитом не даёт такой гарантии. Нужен источник истины, который
  по определению является полной историей.

  Вот эти четыре боли — фундамент, на котором стоят CQRS и ES.
  Без них паттерны бессмысленны.

  2. CQRS — РАЗДЕЛЕНИЕ ПУТЕЙ
  CQRS расшифровывается как Command Query Responsibility
  Segregation. Это не про базы данных. Это про разделение
  обязанностей в архитектуре приложения.

  Идея восходит к принципу CQS (Command-Query Separation)
  Бертрана Мейера: "каждый метод должен либо что-то делать,
  либо что-то возвращать, но не то и другое одновременно".
  CQRS поднимает этот принцип с уровня метода на уровень
  всей системы.

  Что это значит на практике?

  Есть два принципиально разных типа взаимодействия с системой.
  Первый — КОМАНДЫ. Это намерения что-то изменить. "Создать
  заказ", "оплатить счёт", "забронировать место". Команды
  могут быть отклонены. Они меняют состояние. Они требуют
  валидации, применения бизнес-правил, проверки инвариантов.

  Второй тип — ЗАПРОСЫ. Это вопросы о состоянии. "Покажи мои
  заказы", "какой баланс на счету", "сколько товаров на складе".
  Запросы никогда не меняют данные. Они только читают.
  Они оптимизированы под конкретные сценарии чтения.

  В традиционном CRUD-подходе эти два типа взаимодействия
  живут в одной модели данных, в одних и тех же сервисах,
  в одних и тех же хендлерах. CQRS говорит: разделяй их.

  Write Model — это нормализованная база данных, оптимизированная
  под консистентность и запись. Здесь важна атомарность,
  блокировки, транзакции. Это может быть PostgreSQL с жёсткими
  ограничениями целостности.

  Read Model — это денормализованная база данных, оптимизированная
  под конкретные сценарии чтения. Здесь нет JOIN'ов на лету,
  здесь данные уже лежат в той форме, в которой их нужно отдать
  клиенту. Это может быть Redis, Elasticsearch, MongoDB, или
  просто плоская таблица в PostgreSQL.

  Важный момент: Read Model — это НЕ копия Write Model. Это
  отдельная структура, спроектированная под конкретный экран,
  конкретный API, конкретную роль пользователя. Можно строить
  одну Read Model на экран, другую на мобильное API, третью
  на аналитику. Это не дублирование — это специализация.

  Зачем это нужно?

  Во-первых, НЕЗАВИСИМОЕ МАСШТАБИРОВАНИЕ. Если у тебя 1000
  чтений на одну запись, ты можешь масштабировать Read Side
  отдельно. Добавить реплики, кэш, шардировать чтения. Write
  Side остаётся простым и консистентным.

  Во-вторых, РАЗНЫЕ МОДЕЛИ ДАННЫХ. Write Side может быть
  строго нормализованным PostgreSQL. Read Side — Elasticsearch
  для поиска, Redis для кэша, MongoDB для документов. Каждая
  сторона использует инструмент, оптимальный для своей задачи.

  В-третьих, РАЗНАЯ БЕЗОПАСНОСТЬ. К командам одни права,
  к запросам другие. Можно строить granular access control
  отдельно для каждой стороны.

  В-четвёртых, РАЗНАЯ ПРОИЗВОДИТЕЛЬНОСТЬ. Write Side
  оптимизирован под консистентность и валидацию. Read Side —
  под скорость и денормализацию.

  Главный минус CQRS — EVENTUAL CONSISTENCY. Read Model
  обновляется асинхронно. После успешной записи команды данные
  в Read Model могут обновиться не сразу. Это не баг, это
  осознанный trade-off. Взамен ты получаешь масштабируемость
  и производительность.

  Если бизнесу нужна строгая консистентность сразу после
  записи — CQRS может не подойти. Или подойти только на части
  сценариев. Например, для критичных операций использовать
  стратегию "read your own writes" — после команды читать
  из Write Model, а не из Read.

  На собеседовании важно понимать: CQRS — это не "использовать
  две базы данных". Это архитектурный принцип. Можно применить
  CQRS и в одной БД, разделив модели логически.

  3. EVENT SOURCING — СОСТОЯНИЕ КАК ИСТОРИЯ
  Event Sourcing — это подход к хранению данных, при котором
  источником истины является не текущее состояние, а полная
  последовательность событий, которые к этому состоянию привели.

  Формальное определение звучит так:

    Application state is an append-only log of domain events;
    current state is a derived view, not the source of truth.

  То есть текущее состояние — это производная величина.
  Его можно вычислить, но оно не хранится как первичные данные.
  Первичные данные — это поток событий.

  Как это работает на примере банковского счёта?

  В CRUD-мире мы делаем UPDATE balance = 400. Старое значение
  500 исчезает. В Event Sourcing мы записываем СОБЫТИЯ:

    AccountOpened    { account_id: "A1", owner: "Alice" }
    MoneyDeposited   { account_id: "A1", amount: 500 }
    MoneyTransferred { from: "A1", to: "B1", amount: 100 }

  Текущее состояние вычисляется последовательным применением
  всех событий к пустому состоянию. Это называется REPLAY.

  Ключевой инвариант: currentState = events.reduce(applyEvent, initialState)

  Теперь давай разберём ключевые концепции.

  EVENT (СОБЫТИЕ) — это неизменяемый факт. То, что УЖЕ случилось.
  Названия всегда в прошедшем времени: OrderPlaced, ItemAddedToCart,
  PaymentReceived. Событие нельзя изменить или удалить. Если
  нужно "исправить" — пишется новое событие-корректировка.
  Например, CustomerNameCorrected. Ты не отменяешь опечатку,
  ты добавляешь факт исправления.

  EVENT STORE (ХРАНИЛИЩЕ СОБЫТИЙ) — это база данных,
  оптимизированная под append-only операции. Обычно это лог
  или специализированная БД (EventStoreDB, Axon Server).
  Ключевое свойство: события только добавляются, никогда
  не изменяются и не удаляются.

  STREAM (ПОТОК) — это упорядоченная последовательность событий
  для одного агрегата. Например, для заказа *123 — свой stream.
  События внутри одного stream упорядочены строго.

  AGGREGATE (АГРЕГАТ) — это НЕ таблица. Это ГРАНИЦА
  КОНСИСТЕНТНОСТИ. Это то, что загружается в память (replay'ится),
  чтобы принять решение: можно ли выполнить команду. Агрегат
  содержит только те данные, которые нужны для валидации
  команд. Ничего лишнего. Если у тебя агрегат "Order" весом
  в 50 полей, а для валидации команд нужно только 5 — ты
  делаешь что-то не так.

  COMMAND (КОМАНДА) — это намерение что-то сделать. В отличие
  от события, команда может быть ОТКЛОНЕНА. Событие — это
  свершившийся факт. Команда — это "я хочу, чтобы произошло".
  Например, PlaceOrder — команда. OrderPlaced — событие.

  Что даёт Event Sourcing?

  Во-первых, ПОЛНЫЙ АУДИТ. Ты всегда знаешь, что произошло,
  когда и почему. Это не bolt-on аудит, это сам источник истины.

  Во-вторых, ВРЕМЕННЫЕ ЗАПРОСЫ. Можно "отмотать" состояние
  на любой момент времени. "Как выглядел счёт вчера в 15:00?" —
  просто воспроизведи события до этого момента.

  В-третьих, ОТЛАДКА. Нашёл баг в логике — исправь код
  и ПЕРЕИГРАЙ события заново. Состояние исправится. В CRUD-мире
  это невозможно без ручного вмешательства в БД.

  В-четвёртых, ЕСТЕСТВЕННАЯ КОНКУРЕНТНОСТЬ. Запись события —
  это INSERT. Не нужно лочить строки. Конкурентность
  контролируется через ожидаемую версию потока.

  Свойство APPEND-ONLY даёт простую репликацию (как WAL
  в PostgreSQL), возможность читать поток параллельно,
  детерминированный replay.

  4. КАК CQRS И EVENT SOURCING СОЧЕТАЮТСЯ
  Здесь есть важное правило, которое часто путают.

  EVENT SOURCING ПОДРАЗУМЕВАЕТ CQRS. Потому что запрашивать
  состояние напрямую из Event Store — это ад. Представь запрос
  "дай всех пользователей с именем Alice". В Event Store у тебя
  нет таблицы пользователей. У тебя есть поток событий. Чтобы
  ответить на запрос, нужно прочитать и воспроизвести все
  потоки всех пользователей. Это неприемлемо. Значит, ты ОБЯЗАН
  строить отдельные Read Models для запросов.

  Но CQRS НЕ ПОДРАЗУМЕВАЕТ Event Sourcing. Можно разделить
  команды и запросы, но хранить текущее состояние в обычной
  БД. Это тоже CQRS, но без ES.

  Как они работают вместе?

  Write Path (путь записи):
    1. Приходит КОМАНДА (PlaceOrder).
    2. Command Handler загружает АГРЕГАТ, воспроизводя его события.
    3. Проверяет, можно ли выполнить команду.
    4. Если да — генерирует новые СОБЫТИЯ (OrderPlaced).
    5. APPEND эти события в Event Store (с проверкой ожидаемой
       версии).

  Read Path (путь чтения):
    1. ПРОЕКЦИИ (Projections) — фоновые обработчики, которые
       подписываются на поток событий из Event Store.
    2. Они ПРОЕЦИРУЮТ события в READ MODELS — денормализованные,
       оптимизированные структуры.
    3. QUERY HANDLER читает данные из Read Model и возвращает
       клиенту.

  Схема выглядит так:

                     ┌──────────────┐
       Command  ──▶ │   Command    │
                     │   Handler    │
                     └──────┬───────┘
                            ▼
                     ┌──────────────┐
                     │ EVENT STORE  │  ← source of truth
                     └──────┬───────┘
                            ▼
                     ┌──────────────┐
                     │  PROJECTION  │
                     └──────┬───────┘
                            ▼
                     ┌──────────────┐
       Query   ───▶ │  READ MODEL  │
                     └──────────────┘

  В этой схеме Event Store — единственный источник истины.
  Read Models — производные. Их можно удалить и пересобрать
  заново в любой момент.

  5. ПРОЕКЦИИ И READ MODELS — САМАЯ СЛОЖНАЯ ЧАСТЬ
  В Event Sourcing главная сложность — не в записи событий.
  Записать событие просто. Главная сложность — в построении
  и поддержке проекций.

  READ MODEL — ЭТО НЕ КОПИЯ WRITE MODEL. Это отдельная
  структура, спроектированная под конкретные сценарии чтения.

    - Можно построить одну проекцию на экран.
    - Можно один Read Model на роль пользователя.
    - Read Model может быть в другой БД (PostgreSQL, MongoDB,
      Elasticsearch, Redis).
    - Они eventually consistent по дизайну.

  Пример. Для страницы "Мои заказы" ты можешь построить плоскую
  таблицу user_orders_view, которая объединяет данные из
  нескольких потоков (Order, Payment, Shipment). Это будет
  очень быстрый SELECT. Вместо JOIN'ов по пяти таблицам
  и подсчётов на лету — одна денормализованная таблица,
  готовая к отдаче.

  Проекции бывают двух типов.

  INLINE (синхронные) — проекция обновляется в ТОЙ ЖЕ
  транзакции, что и запись события. Гарантирует немедленную
  консистентность. Минус — снижает пропускную способность записи.

  ASYNC (асинхронные) — проекция обновляется фоновым процессом.
  Лучшая производительность и развязка. Минус — eventual
  consistency.

  Одно из главных преимуществ Event Sourcing — REBUILD ПРОЕКЦИЙ.
  Если проекция сломалась или изменилась логика, её можно
  пересобрать с нуля: остановить, сбросить Read Model,
  прогнать все события заново. В CRUD-мире это невозможно
  без потери истории.

  Но именно проекции становятся главным источником боли
  в продакшене. Lag (отставание), ребилд на живом проде,
  версионирование проекций, обработка ошибок — всё это
  требует зрелой инженерии.

  6. КОНСИСТЕНТНОСТЬ, КОНКУРЕНТНОСТЬ, ИДЕМПОТЕНТНОСТЬ
  В распределённых системах невозможно гарантировать exactly-once
  доставку. Можно только at-least-once (минимум один раз).
  Это значит, что ДУБЛИ НЕИЗБЕЖНЫ.

  Решение — ИДЕМПОТЕНТНОСТЬ. У каждого события есть уникальный
  event_id. Консюмер (проекция) перед обработкой проверяет,
  не обрабатывал ли он это событие раньше (через таблицу
  processed_events). Если обрабатывал — пропускает. Если нет —
  обрабатывает и сохраняет event_id В ТОЙ ЖЕ ТРАНЗАКЦИИ,
  что и результат обработки.

  Это даёт ЭФФЕКТ exactly-once: even if message delivered twice,
  side effects applied once.

  ОПТИМИСТИЧНЫЙ КОНТРОЛЬ КОНКУРЕНТНОСТИ. Когда два процесса
  пишут в один поток, Event Store проверяет ОЖИДАЕМУЮ ВЕРСИЮ.
  "Я ожидаю, что текущая версия потока = 5. Если она всё ещё 5 —
  запиши моё событие. Если она уже 6 — верни ошибку
  OptimisticConcurrencyError." Клиент должен перечитать поток
  и попробовать снова.

  Это НЕ пессимистичная блокировка. Мы не держим лок. Мы
  просто проверяем версию в момент записи.

  7. ЭВОЛЮЦИЯ СОБЫТИЙ — ВЕРСИОНИРОВАНИЕ И UPCASTING
  События неизменяемы. Но бизнес-требования меняются. Как
  добавлять новые поля в события, которые уже записаны?

  UPCASTING — это процесс преобразования "старого" события
  в "новый" формат ПРИ ЧТЕНИИ. Ты не переписываешь историю,
  ты адаптируешь её к текущему коду.

  Пример. В 2023 году событие UserRegistered содержало поля
  name и email. В 2024 году бизнес хочет добавить phone_number.
  Когда ты читаешь старое событие UserRegistered без phone_number,
  upcaster подставляет phone_number: null. Когда ты читаешь
  новое событие, phone_number уже есть.

  Важно: не пытайся "исправить" старое событие. Ты не можешь
  изменить прошлое. Ты можешь только написать НОВОЕ
  событие-корректировку.

  Стратегии эволюции схем:

    - WEAK SCHEMA — поля опциональны, добавляй новые без боли.
    - STRONG SCHEMA + UPCASTERS — строгая схема + цепочка
      upcaster'ов от версии N к версии N+1.
    - COPY-AND-REPLACE — перезапись старого потока в новый
      формат (в новой коллекции / топике).

  8. EVENT STORE — ТРЕБОВАНИЯ И ВЫБОР
  Event Store — это НЕ "ещё одна таблица в PostgreSQL". Это
  специализированная подсистема. Минимальные требования:

    - APPEND-ONLY. Только вставка. Никаких UPDATE, никаких DELETE.
    - TOTAL ORDER внутри stream. События одного агрегата
      читаются строго в порядке записи.
    - GLOBAL ORDER (опционально). Для подписок "все события
      после позиции N".
    - OPTIMISTIC LOCK. Проверка expectedVersion при append.
    - SUBSCRIPTIONS. Возможность tail'ить поток.
    - DURABILITY. Запись = сохранено.

  KAFKA КАК EVENT STORE — ОПАСНО. Kafka — отличный брокер,
  но у неё есть retention (срок хранения). Она не предназначена
  для хранения всей истории вечно. Compaction удаляет историю.
  Нет понятия "stream per aggregate" из коробки. Нет версий
  для optimistic concurrency.

  Где хранить:

    - EventStoreDB, Axon Server — специализированные решения.
    - PostgreSQL — таблица events, UNIQUE(stream_id, version).
      Это и есть optimistic lock. Глобальный порядок — через
      BIGSERIAL global_position. Работает до десятков тысяч
      событий в секунду на одном узле.
    - Kafka — транспорт проекций, не source of truth.

  9. МАСШТАБИРОВАНИЕ — HOT AGGREGATES, ШАРДИРОВАНИЕ, LAG
  HOT AGGREGATE — это главная боль Event Sourcing. Представь
  счёт популярного продавца, куда летят все операции. Один
  stream, куда пишут тысячи процессов одновременно. Конфликты
  версий становятся нормой. Retry storm.

  Лечение:

    - КАЖДАЯ ТРАНЗАКЦИЯ — свой stream (txn-456). Баланс —
      проекция, не агрегат.
    - Агрегат держит ТОЛЬКО инварианты (лимиты, статусы),
      а не всю историю операций.
    - Idempotency key на команду — повтор не создаёт дубль.
    - Backoff + jitter при конфликтах.

  ШАРДИРОВАНИЕ. Streams можно шардировать по stream_id (hash).
  Одна партиция = один writer = нет contention. Проекции читают
  партиции параллельно.

  Правило выбора ключа партиции:

    - По aggregate_id (order_id) — per-stream порядок.
    - По tenant_id — изоляция и локальность данных.
    - По user_id — если проекции "мои заказы".

  PROJECTIONS AT SCALE. Ключевая метрика — LAG. Это позиция
  в event stream минус позиция последнего обработанного события
  в проекции. Измеряется в событиях ИЛИ во времени.

  Если lag растёт — проекция не успевает — eventual consistency
  превращается в "never consistency".

  Алерты:

    - lag > 10 000 событий.
    - lag > 30 секунд.
    - lag растёт монотонно (проекция мертва).

  Стратегии:

    - Параллелизм по stream (партиция = consumer).
    - Batching.
    - Snapshot-based replay.
    - Checkpointing.

  РЕБИЛД ПРОЕКЦИИ НА ПРОДЕ. Правильный порядок:

    1. Создать НОВУЮ версию проекции рядом (не ломая старую).
    2. Начать replay с нуля в неё.
    3. Прогревать её до минимального lag.
    4. Переключить трафик read API на новую.
    5. Старую удалить.

  Антипаттерн: DROP TABLE + replay на проде. Это способ
  положить продакшен на несколько часов.

  10. GDPR И ПРАВО НА ЗАБВЕНИЕ
  Event Sourcing говорит: события неизменяемы. GDPR говорит:
  удали персональные данные по запросу пользователя. Конфликт.

  Решения:

    - PII OUT OF EVENTS. Персональные данные хранятся отдельно,
      а в событиях — только ID. Удаление ID = удаление связи.

    - CRYPTO-SHREDDING. PII шифруется ключом на пользователя.
      Удаление = удаление ключа. Событие остаётся, но
      расшифровать его нельзя.

    - ANONYMIZATION AT READ. События содержат псевдонимы,
      реальные данные — в отдельном сервисе.

  На собеседовании важно сказать: "Мы держим PII вне событий.
  События ссылаются на user_id. Если приходит запрос на
  удаление — удаляем PII из отдельного хранилища. История
  событий сохраняется для compliance, но без персональных
  данных."

  11. PROCESS MANAGERS — САГИ НА СОБЫТИЯХ
  Process Manager — это компонент, который слушает события
  и выпускает команды. Координирует долгоживущий бизнес-процесс.

  Отличие от Saga (см. урок 8.3):

    - Saga — последовательность шагов с компенсациями.
    - Process Manager — долгоживущее состояние, реагирующее
      на события из разных агрегатов.

  Пример. Process Manager "OrderFulfillment" слушает события
  OrderPlaced, PaymentReserved, PaymentFailed, StockReserved,
  StockFailed. И выпускает команды ReservePayment,
  ReserveStock, RefundPayment, ConfirmOrder.

  Process Manager хранит своё состояние — обычно в event-sourced
  виде (собственный stream). Позволяет переиграть процесс
  после падения.

  12. МИГРАЦИЯ С CRUD НА ES
  НЕ ДЕЛАЙ BIG BANG. Переписать всю систему на ES за один
  релиз = катастрофа. Никто не умеет с ним работать, метрики
  не настроены, проекции отстают, консьюмеры падают.

  STRANGLER FIG ДЛЯ ES:

    1. Выбрать ОДИН агрегат (например, Order).
    2. Ввести ES только для него, остальное — CRUD.
    3. Настроить проекции в существующие таблицы.
    4. Постепенно перевести API на новую модель.
    5. Повторить с следующим агрегатом.

  DUAL WRITE КАК ПЕРЕХОДНЫЙ ПЕРИОД. Пишем и в CRUD-таблицу,
  и в Event Store. Проекция читает из Event Store и обновляет
  CRUD. Через N месяцев — удаляем прямой write в CRUD.
  Dual write — временная мера. Долго так жить нельзя.

  BACKFILL ИСТОРИИ. Есть текущее состояние в CRUD. Как получить
  события? Либо сгенерировать одно событие StateImported
  с текущим состоянием (теряется история), либо восстановить
  историю из audit log (если есть).

  13. КОГДА ИСПОЛЬЗОВАТЬ И КОГДА НЕ ИСПОЛЬЗОВАТЬ
  ЗЕЛЁНЫЕ ФЛАГИ. Используй CQRS и ES, когда:

    - НУЖЕН ПОЛНЫЙ АУДИТ ДЛЯ COMPLIANCE. Финансы, медицина,
      юриспруденция — везде, где нужно доказывать: кто, что
      и когда сделал.

    - НУЖНО ВОССТАНАВЛИВАТЬ ПРОШЛЫЕ СОСТОЯНИЯ. Разбор споров,
      анализ, отладка сложных багов.

    - СЛОЖНАЯ БИЗНЕС-ЛОГИКА С БОГАТОЙ ИСТОРИЕЙ. Заказы,
      платежи, workflow — где важна последовательность
      и причины изменений.

    - ВЫСОКИЕ ТРЕБОВАНИЯ К ПРОПУСКНОЙ СПОСОБНОСТИ ЗАПИСИ.
      Append-only масштабируется лучше, чем UPDATE с блокировками.

    - НУЖНО СТРОИТЬ МНОГО РАЗНЫХ READ MODELS. CQRS даёт
      гибкость: одна модель для поиска, другая для аналитики,
      третья для мобильного API.

    - КОМАНДА ≥ 15 ЧЕЛОВЕК И ЗРЕЛЫЕ ИНЖЕНЕРЫ. Event Sourcing
      требует опыта: конкурентность, идемпотентность,
      версионирование, снапшоты, projections lag.

  КРАСНЫЕ ФЛАГИ. НЕ используй Event Sourcing, когда:

       КОМАНДА < 10-15 ЧЕЛОВЕК. Получишь всю сложность,
       но не выгоду. Для поддержки ES нужны зрелые инженеры.

       "ПОТОМУ ЧТО МОДНО". Если не можешь назвать конкретную
       проблему — не делай.

       ТЕКУЩЕЕ СОСТОЯНИЕ — ЭТО ВСЁ, ЧТО НУЖНО. Корзина покупок,
       настройки пользователя. История изменений не важна.

       ВЫСОКОЧАСТОТНЫЕ ИЗМЕРЕНИЯ. IoT-сенсоры, GPS-трекинг,
       логи. Миллионы событий в секунду — это не Event Sourcing,
       это потоковая аналитика.

       ПРОСТЫЕ CRUD-ОПЕРАЦИИ. Если нужно создавать/читать/
       обновлять/удалять и не париться об истории — CRUD
       быстрее и проще.

       "EVENT SOURCING ВЕЗДЕ". Самая большая ошибка — построить
       всю систему на ES. Это антипаттерн "Event-Sourced Monolith".

       ИГНОРИРОВАНИЕ GDPR. В ES нельзя просто "удалить" событие.
       Нужно продумывать стратегии анонимизации или
       криптошрединга.

  ЕСЛИ СОМНЕВАЕШЬСЯ — начни с монолита. Начни с CRUD.
  Потом выдели ОДИН-ДВА агрегата и примени CQRS/ES там,
  где это реально нужно.

  14. АНТИПАТТЕРНЫ

  14.1. СЧИТАТЬ, ЧТО АГРЕГАТ — ЭТО ТАБЛИЦА.
    Агрегат — граница для принятия решений. Он содержит только
    то, что нужно для валидации команд.
  14.2. ЗАПИСЫВАТЬ CommandFailed В EVENT STORE.
    Событие — факт о том, что ПРОИЗОШЛО В БИЗНЕСЕ.
    Ошибка валидации — это шум инфраструктуры. Храни ошибки
    в логах.
  14.3. ПОДПИСЫВАТЬСЯ НА ОДНО СОБЫТИЕ НЕСКОЛЬКО РАЗ.
    Ведёт к дублированию действий при retry.
  14.4. "КОМПЕНСИРУЮЩЕЕ СОБЫТИЕ ИСПРАВИТ ВСЁ".
    Компенсация (refund) — правильный подход для отмены
    транзакции. Но для исправления опечатки — это неверно.
    Ты не "отменяешь" опечатку, ты пишешь новое событие-
    корректировку.
  14.5. ИСПОЛЬЗОВАТЬ KAFKA КАК EVENT STORE.
    Kafka — брокер, не хранилище истории.
  14.6. REPLAY НА ПРОДЕ БЕЗ ПРЕДОХРАНИТЕЛЕЙ.
    Пересборка проекции на живой системе без rate-limiting
    и без изолированного окружения — способ положить прод.
  14.7. ОТСУТСТВИЕ IDEMPOTENCY В ПРОЕКЦИЯХ.
    At-least-once → дубли → сломанные проекции.
  14.8. ВСЁ В ОДНОМ АГРЕГАТЕ.
    Если у тебя один агрегат "App" и через него идут все
    операции — ты построил ES-монолит.

  15. ТЕСТИРОВАНИЕ, МОНИТОРИНГ, ТРАССИРОВКА
  ТЕСТИРОВАНИЕ. GIVEN-WHEN-THEN идеально для ES.

    GIVEN:  поток событий (состояние)
    WHEN:   команда
    THEN:   список новых событий

  Тестируй логику без БД, очередей и проекций. Обязательно
  тестируй:

    - идемпотентность проекций,
    - поведение при OptimisticConcurrencyError,
    - upcasting старых событий.

  МОНИТОРИНГ.

    WRITE SIDE:
      - количество обработанных команд,
      - количество отклонённых команд,
      - latency записи событий,
      - optimistic_lock_conflicts_total,
      - aggregate_load_events (сколько событий replay'ится
        для загрузки агрегата).

    READ SIDE:
      - projection_lag_events (ключевая метрика),
      - projection_lag_seconds,
      - projection_processed_events_total,
      - projection_errors_total,
      - projection_rebuild_duration_seconds.

    EVENT STORE:
      - events_written_total,
      - events_read_total,
      - storage_size_bytes,
      - streams_count.

  АЛЕРТЫ:

    - projection_lag_seconds > 60 → page.
    - projection_lag_growing (не уменьшается 15 мин) → page.
    - event_append_errors_total > threshold → page.
    - optimistic_lock_conflicts_total вырос в 10 раз → ticket.

  ТРАССИРОВКА. Используй correlation_id и causation_id.
  Correlation_id — общий для всей бизнес-операции. Causation_id —
  id события/команды, которая вызвала текущее. Оба должны быть
  в METADATA события, а не в payload. Payload — то, что важно
  домену. Metadata — то, что важно инфраструктуре.

  Антипаттерн: "кладём trace_id в payload события". Через год
  trace_id в payload станет legacy-полем, которое непонятно
  зачем лежит в контракте домена.

  16. ФИНАЛЬНЫЕ ВЫВОДЫ

  1.  ES — не "ещё один паттерн", а смена парадигмы:
      не состояние, а поток событий — источник истины.
  2.  CQRS и ES — точечные инструменты, не архитектура
      всей системы. "ES везде" — самая дорогая ошибка.
  3.  Event Store — специализированная подсистема.
      Kafka им не является.
  4.  Глобальный порядок — дорого. Per-stream порядка
      почти всегда достаточно.
  5.  Optimistic concurrency + идемпотентность —
      обязательные свойства любой ES-системы.
  6.  Проекции — главный источник боли. Lag, rebuild,
      версионирование — обязательные практики.
  7.  События — публичные контракты на десятилетия.
      Schema registry и contract-first — не опция.
  8.  GDPR решается через crypto-shredding, PII out of events
      и анонимизацию на чтении.
  9.  Миграция — только через Strangler Fig и один агрегат
      за раз.
  10. Сложность ES оправдана только там, где нужен audit trail,
      временные запросы и богатая история.
  11. На Middle+ уверенно объясняй базовые концепции
      и trade-off'ы.
  12. На Senior показывай понимание performance, consistency,
      security, миграции и failure modes.
  13. Начинай с CRUD. Выделяй ES там, где больно.
      Не строй ES-монолит.
  14. ES — это десятилетний контракт. Проектируй события так,
      будто они будут жить вечно. Потому что будут.
*/
