package main

/*
  PROTOCOL BUFFERS
  В этом файле — ВСЯ теория по protobuf, которая может понадобиться
  на собеседовании и в реальной работе. Всё собрано в одном месте,
  структурировано, с примерами и пояснениями.

  СОДЕРЖАНИЕ:
    1.  Что такое Protocol Buffers и зачем они нужны
    2.  Синтаксис proto3 (базовый)
    3.  Пакеты (package) и импорты (import)
    4.  Сообщения (message) и поля
    5.  Скалярные типы данных
    6.  Номера полей и Wire Format (как это работает внутри)
    7.  Специальные конструкции: oneof, map, enum, repeated
    8.  reserved — защита от переиспользования
    9.  optional и presence tracking
    10. Well-known types (Timestamp, Duration, Empty, Any, Struct, FieldMask)
    11. Вложенные сообщения (nested messages)
    12. Сервисы (service) и RPC-методы
    13. JSON-маппинг для protobuf
    14. Text Format (человекочитаемый вывод)
    15. Опции полей: json_name, deprecated, packed
    16. Кастомные опции (Custom Options)
    17. Сравнение proto2 vs proto3 и Editions
    18. Генерация кода (protoc + плагины)
    19. Backward compatibility — как менять схему без поломки
    20. Best practices и типичные ошибки
    21. Часто задаваемые вопросы на собеседованиях

  Весь материал — из официальной документации protobuf.dev.

  1.  ЧТО ТАКОЕ PROTOCOL BUFFERS И ЗАЧЕМ ОНИ НУЖНЫ
  Protocol Buffers (protobuf) — это механизм сериализации структурированных
  данных, разработанный Google. Он используется для:
    • Описания структуры данных в .proto-файлах.
    • Генерации кода для множества языков (Go, Java, Python, C++...).
    • Эффективной бинарной сериализации (размер меньше JSON, скорость выше).

  Основные преимущества:
    • Компактность: бинарный формат, сообщения в 3-10 раз меньше JSON.
    • Скорость: сериализация/десериализация в разы быстрее JSON.
    • Типобезопасность: строгий контракт, генерация кода.
    • Совместимость: обратная и прямая совместимость при правильной эволюции.
    • Языковая нейтральность: один .proto для всех языков.

  В контексте gRPC protobuf используется как IDL (Interface Definition Language)
  для описания сервисов и сообщений.

  2.  СИНТАКСИС PROTO3 (БАЗОВЫЙ)
  Каждый .proto-файл начинается с указания версии синтаксиса:
    syntax = "proto3";
  Это должна быть первая непустая, некомментная строка.

  3.  ПАКЕТЫ (PACKAGE) И ИМПОРТЫ (IMPORT)
  3.1. package — пространство имён
    package myapp.users.v1;

  Зачем:
    • Предотвращает конфликты имён между разными .proto-файлами.
    • Определяет пространство имён в сгенерированном коде.

  Правила:
    • Только lowercase, слова через точки.
    • Должен быть уникальным для вашего проекта.

  3.2. option go_package — для Go
    option go_package = "github.com/myorg/api/user/v1;userv1";
    имя пакета в Go

  БЕЗ ЭТОГО ПРОТОК НЕ СГЕНЕРИРУЕТ КОД ДЛЯ GO.

  3.3. import — подключение других .proto-файлов
    import "google/protobuf/timestamp.proto";
    import "myapp/common/common.proto";

  • import public "..." — делает импорт транзитивным (если кто-то импортирует
    твой файл, он увидит и public-зависимости).
  • import weak "..." — слабая зависимость (используется редко, для
    предотвращения циклических импортов).

  4.  СООБЩЕНИЯ (MESSAGE) И ПОЛЯ
  4.1. Определение сообщения
    message SearchRequest {
      string query = 1;
      int32 page_number = 2;
      int32 results_per_page = 3;
    }

  Поле состоит из:
    • Типа (string, int32, ...)
    • Имени (query)
    • Номера поля (1, 2, 3) — ОБЯЗАТЕЛЬНО уникальный в пределах сообщения.

  4.2. Номера полей (Field Numbers)
  • Диапазон: 1–536,870,911.
  • Номера 19,000–19,999 зарезервированы для protobuf.
  • Номера 1–15 кодируются в 1 байт (используй для частых полей).
  • Номера 16–2047 — в 2 байта.
  • Номера НЕЛЬЗЯ менять после того, как сообщение используется в продакшене.

  5.  СКАЛЯРНЫЕ ТИПЫ ДАННЫХ
  Полная таблица соответствия .proto → Go:
  ┌─────────────────────┬─────────────┬───────────────────────────────────────┐
  │ .proto Тип          │ Go Тип      │ Примечания                            │
  ├─────────────────────┼─────────────┼───────────────────────────────────────┤
  │ double              │ float64     │                                       │
  ├─────────────────────┼─────────────┼───────────────────────────────────────┤
  │ float               │ float32     │                                       │
  ├─────────────────────┼─────────────┼───────────────────────────────────────┤
  │ int32               │ int32       │ Для отрицательных чисел лучше sint32  │
  ├─────────────────────┼─────────────┼───────────────────────────────────────┤
  │ int64               │ int64       │ Для отрицательных лучше sint64        │
  ├─────────────────────┼─────────────┼───────────────────────────────────────┤
  │ uint32              │ uint32      │                                       │
  ├─────────────────────┼─────────────┼───────────────────────────────────────┤
  │ uint64              │ uint64      │                                       │
  ├─────────────────────┼─────────────┼───────────────────────────────────────┤
  │ sint32              │ int32       │ Эффективнее для отрицательных чисел   │
  ├─────────────────────┼─────────────┼───────────────────────────────────────┤
  │ sint64              │ int64       │ Эффективнее для отрицательных чисел   │
  ├─────────────────────┼─────────────┼───────────────────────────────────────┤
  │ fixed32             │ uint32      │ Всегда 4 байта (эффективно для битов) │
  ├─────────────────────┼─────────────┼───────────────────────────────────────┤
  │ fixed64             │ uint64      │ Всегда 8 байт                         │
  ├─────────────────────┼─────────────┼───────────────────────────────────────┤
  │ sfixed32            │ int32       │ Всегда 4 байта (со знаком)            │
  ├─────────────────────┼─────────────┼───────────────────────────────────────┤
  │ sfixed64            │ int64       │ Всегда 8 байт (со знаком)             │
  ├─────────────────────┼─────────────┼───────────────────────────────────────┤
  │ bool                │ bool        │                                       │
  ├─────────────────────┼─────────────┼───────────────────────────────────────┤
  │ string              │ string      │ UTF-8, не более 2^32 байт             │
  ├─────────────────────┼─────────────┼───────────────────────────────────────┤
  │ bytes               │ []byte      │ Произвольные байты, не более 2^32     │
  └─────────────────────┴─────────────┴───────────────────────────────────────┘

  6.  НОМЕРА ПОЛЕЙ И WIRE FORMAT (КАК ЭТО РАБОТАЕТ ВНУТРИ)
  Это САМОЕ ВАЖНОЕ для понимания эффективности protobuf.

  6.1. Varint (Variable-length integer)
  • Маленькие числа (0-127) занимают 1 байт.
  • Большие числа занимают больше байт.
  • Каждый байт имеет continuation bit (MSB):
      - 1 — есть ещё байты.
      - 0 — последний байт.

  Пример: число 150 кодируется как `96 01`.

  6.2. Wire Types — как protobuf понимает, что за данные
  Key = (field_number << 3) | wire_type

  ┌─────────────────────┬─────────────────────────────────────────────────────┐
  │ Wire Type           │ Назначение                                          │
  ├─────────────────────┼─────────────────────────────────────────────────────┤
  │ 0 (VARINT)          │ int32, int64, uint32, uint64, sint32, sint64,       │
  │                     │ bool, enum                                          │
  ├─────────────────────┼─────────────────────────────────────────────────────┤
  │ 1 (I64)             │ fixed64, sfixed64, double                           │
  ├─────────────────────┼─────────────────────────────────────────────────────┤
  │ 2 (LEN)             │ string, bytes, embedded messages, packed            │
  │                     │ repeated fields                                     │
  ├─────────────────────┼─────────────────────────────────────────────────────┤
  │ 3 (SGROUP)          │ устаревший (group)                                  │
  ├─────────────────────┼─────────────────────────────────────────────────────┤
  │ 4 (EGROUP)          │ устаревший (end group)                              │
  ├─────────────────────┼─────────────────────────────────────────────────────┤
  │ 5 (I32)             │ fixed32, sfixed32, float                            │
  └─────────────────────┴─────────────────────────────────────────────────────┘

  6.3. Packed repeated
  Для повторяемых числовых полей (repeated int32, repeated float и т.д.)
  используется упаковка (packed encoding) — все значения помещаются в одно
  поле с wire type LEN, что экономит место.

    repeated int32 values = 1 [packed = true];  // по умолчанию true в proto3

  7.  СПЕЦИАЛЬНЫЕ КОНСТРУКЦИИ: ONEOF, MAP, ENUM, REPEATED
  7.1. repeated — повторяемые поля (слайс/массив)
    message User {
      string id = 1;
      repeated string roles = 2;  // []string в Go
      repeated Email emails = 3;  // []*Email в Go
    }

  Имена повторяемых полей должны быть во множественном числе.

  7.2. oneof — эксклюзивный выбор (только одно поле из набора)
    message Notification {
      string user_id = 1;
      oneof content {
        string text = 2;
        string image_url = 3;
        Video video = 4;
      }
    }

  Ограничения:
    • Нельзя использовать repeated или map внутри oneof.
    • Если нужно repeated — оберни в отдельное сообщение.

  В Go генерируется как интерфейс или структура с указателями.

  7.3. map — ассоциативный массив
    message User {
      string id = 1;
      map<string, string> metadata = 2;  // map[string]string в Go
    }

  Ограничения:
    • Ключи — только скалярные типы (кроме float, bytes).
    • Нельзя использовать repeated с map.

  7.4. enum — перечисления
    enum Status {
      STATUS_UNSPECIFIED = 0;  // ОБЯЗАТЕЛЬНО: значение по умолчанию
      STATUS_ACTIVE = 1;
      STATUS_INACTIVE = 2;
    }

  Правила:
    • Первое значение ДОЛЖНО быть 0 с суффиксом UNSPECIFIED.
    • Нельзя удалять значения enum без reserved.
    • При получении неизвестного значения, оно сохраняется как unknown.

  8.  RESERVED — ЗАЩИТА ОТ ПОВТОРНОГО ИСПОЛЬЗОВАНИЯ
  Когда вы удаляете поле, его номер НЕЛЬЗЯ использовать повторно.

    message User {
      reserved 2, 15, 9 to 11;       // зарезервированные номера
      reserved "old_field_name";     // зарезервированные имена
      string id = 1;
      string name = 3;
    }

  Для enum тоже есть reserved:

    enum Status {
      STATUS_UNSPECIFIED = 0;
      STATUS_ACTIVE = 1;
      reserved 2, 3;
      reserved "STATUS_INACTIVE";
    }

  9.  OPTIONAL И PRESENCE TRACKING
  В proto3 все поля по умолчанию являются optional (необязательными),
  но до версии 3.15 не было явного синтаксиса для различения "не установлено"
  и "установлено в значение по умолчанию".

  С proto3.15 появился тег optional:

    message User {
      string id = 1;
      optional string name = 2;  // явное указание необязательности
    }

  В Go это генерируется как указатель (*string), что позволяет отличить
  nil (не установлено) от "" (установлено в пустую строку).

  10. WELL-KNOWN TYPES (WKT)
  Это стандартные сообщения, которые поставляются вместе с protobuf.

  ┌─────────────────────┬─────────────────────────────────────────────────────┐
  │ Тип                 │ Назначение                                          │
  ├─────────────────────┼─────────────────────────────────────────────────────┤
  │ google.protobuf.Timestamp │ Точка во времени (наносекундная точность)     │
  ├─────────────────────┼─────────────────────────────────────────────────────┤
  │ gprotobuf.Duration  │ Интервал времени                                    │
  ├─────────────────────┼─────────────────────────────────────────────────────┤
  │ gprotobuf.Empty     │ Пустое сообщение (аналог void)                      │
  ├─────────────────────┼─────────────────────────────────────────────────────┤
  │ gprotobuf.Any       │ Любое сообщение (динамический тип)                  │
  ├─────────────────────┼─────────────────────────────────────────────────────┤
  │ gprotobuf.Struct    │ Динамическая структура (как JSON объект)            │
  ├─────────────────────┼─────────────────────────────────────────────────────┤
  │ gprotobuf.FieldMask │ Маска полей для частичного обновления               │
  └─────────────────────┴─────────────────────────────────────────────────────┘

  Импорт для использования:
    import "google/protobuf/timestamp.proto";
    import "google/protobuf/duration.proto";
    import "google/protobuf/empty.proto";
    import "google/protobuf/any.proto";
    import "google/protobuf/struct.proto";
    import "google/protobuf/field_mask.proto";

  11. ВЛОЖЕННЫЕ СООБЩЕНИЯ (NESTED MESSAGES)
  Сообщения могут быть вложенными.

    message Outer {
      message Inner {
        string value = 1;
      }
      Inner inner = 1;
    }

  Используется, когда сообщение имеет смысл только внутри другого.

  12. СЕРВИСЫ (SERVICE) И RPC-МЕТОДЫ
  service — определяет набор RPC-методов для gRPC.

    service UserService {
      rpc GetUser (GetUserRequest) returns (User) {}
      rpc ListUsers (ListUsersRequest) returns (stream User) {}  // server-streaming
      rpc CreateUsers (stream CreateUserRequest) returns (CreateUsersResponse) {}  // client-streaming
      rpc Chat (stream ChatMessage) returns (stream ChatMessage) {}  // bidirectional
      rpc DeleteUser (GetUserRequest) returns (google.protobuf.Empty) {}
    }

  • stream — указывает на стриминг.
  • google.protobuf.Empty — для методов, которые не возвращают данных.

  13. JSON-МАППИНГ ДЛЯ PROTOBUF
  Protobuf имеет стандартное JSON-отображение, которое используется
  в grpc-gateway и для отладки.

  • google.protobuf.Timestamp → "2024-01-01T00:00:00Z"
  • google.protobuf.Duration → "1.234s"
  • google.protobuf.Struct → {"key": "value"}
  • enum → строковое имя ("ACTIVE")
  • repeated → массив
  • map → объект
  • message → объект

  14. TEXT FORMAT (ЧЕЛОВЕКОЧИТАЕМЫЙ ВЫВОД)
  Protobuf поддерживает текстовый формат (не JSON), используемый для
  конфигов и отладки.

    user {
      id: 1
      name: "Alice"
      status: STATUS_ACTIVE
    }

  В Go можно использовать `proto.MarshalTextString()`.

  15. ОПЦИИ ПОЛЕЙ: JSON_NAME, DEPRECATED, PACKED
  15.1. json_name — задаёт имя поля в JSON

    string user_id = 1 [json_name = "userId"];

  15.2. deprecated — помечает поле как устаревшее

    string old_field = 1 [deprecated = true];

  15.3. packed — для повторяемых числовых полей (по умолчанию true в proto3)

    repeated int32 values = 1 [packed = true];

  16. КАСТОМНЫЕ ОПЦИИ (CUSTOM OPTIONS)
  Позволяют расширять protobuf собственными метаданными.

    import "google/protobuf/descriptor.proto";

    extend google.protobuf.FieldOptions {
      string my_option = 50000;
    }

    message User {
      string id = 1 [(my_option) = "hello"];
    }

  Используется в grpc-gateway (google.api.http) и валидаторах.

  17. СРАВНЕНИЕ PROTO2 VS PROTO3 И EDITIONS
  ┌─────────────────────┬──────────────────────┬────────────────────────────┐
  │ Характеристика      │ proto2               │ proto3                     │
  ├─────────────────────┼──────────────────────┼────────────────────────────┤
  │ required/optional   │ required, optional   │ только optional            │
  ├─────────────────────┼──────────────────────┼────────────────────────────┤
  │ default             │ можно задать         │ фиксированные(0, "", false)│
  ├─────────────────────┼──────────────────────┼────────────────────────────┤
  │ oneof               │ да                   │ да                         │
  ├─────────────────────┼──────────────────────┼────────────────────────────┤
  │ extensions          │ да                   │ нет (заменён Any)          │
  ├─────────────────────┼──────────────────────┼────────────────────────────┤
  │ JSON mapping        │ нет                  │ да (стандартное)           │
  └─────────────────────┴──────────────────────┴────────────────────────────┘

  Editions (выпуски) — новая фича, которая позволяет смешивать фичи
  proto2 и proto3 в одном файле. Появились с protobuf v21+.

  18. ГЕНЕРАЦИЯ КОДА (PROTOC + ПЛАГИНЫ)
  Установка плагинов:

    go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
    go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest

  Генерация:

    protoc --proto_path=proto \
           --go_out=. --go_opt=paths=source_relative \
           --go-grpc_out=. --go-grpc_opt=paths=source_relative \
           proto/user.proto

  Другие полезные плагины:
    • protoc-gen-validate — валидация
    • protoc-gen-grpc-gateway — REST API
    • protoc-gen-doc — документация
    • protoc-gen-go-json — кастомная JSON-сериализация

  19. BACKWARD COMPATIBILITY — КАК МЕНЯТЬ СХЕМУ БЕЗ ПОЛОМКИ
   БЕЗОПАСНЫЕ ИЗМЕНЕНИЯ (не ломают совместимость):
    • Добавление новых полей (с новыми номерами).
    • Удаление полей (с reserved).
    • Добавление новых значений в enum (с новыми номерами).
    • Добавление новых методов в service.
    • Добавление новых oneof-полей.

   ОПАСНЫЕ ИЗМЕНЕНИЯ (ломают совместимость):
    • Изменение номера поля.
    • Изменение типа поля.
    • Удаление поля без reserved.
    • Изменение семантики поля.
    • Удаление enum значения без reserved.
    • Изменение имени поля (меняет JSON-маппинг).

  ПРАВИЛО БОЛЬШОГО ПАЛЬЦА:
    • Добавляй — безопасно.
    • Удаляй — с reserved.
    • Меняй — нельзя.

  20. BEST PRACTICES И ТИПИЧНЫЕ ОШИБКИ
   BEST PRACTICES:
    • Используй номера 1-15 для частых полей.
    • Всегда указывай go_package для Go.
    • Добавляй reserved для удалённых полей.
    • Используй enum с UNSPECIFIED в нулевом значении.
    • Документируй сообщения и поля.
    • Храни .proto-файлы в общем репозитории.
    • Версионируй API через пакеты (v1, v2).
    • Используй buf для линтинга и форматирования.

   ЧАСТЫЕ ОШИБКИ (КРАСНЫЙ ФЛАГ НА СОБЕСЕДОВАНИИ):
    • Изменение номера поля после продакшена.
    • Повторное использование номера удалённого поля.
    • Изменение типа поля.
    • Удаление enum значения без reserved.
    • Игнорирование unknown enum значений.
    • Создание сообщений с десятками полей (плохая читаемость).
    • Отсутствие go_package.

  21. ЧАСТО ЗАДАВАЕМЫЕ ВОПРОСЫ
  Q1: Почему номера полей 1-15 кодируются в 1 байт?
  A1: Потому что ключ (key) кодируется как (field_number << 3) | wire_type.
      Для чисел 1-15 это даёт значение < 128 → 1 байт.

  Q2: Можно ли изменить номер поля после продакшена?
  A2: НЕТ. Это сломает десериализацию старых данных.

  Q3: Как защититься от повторного использования удалённых полей?
  A3: Использовать reserved.

  Q4: Чем oneof отличается от enum?
  A4: oneof — для данных разных типов (структур), enum — для выбора из
      заранее известных констант (категорий).

  Q5: Что такое optional в proto3?
  A5: Это явное указание, что поле необязательное. В Go генерируется
      как указатель, что позволяет отличить "не установлено" от "значение
      по умолчанию".

  Q6: В чём разница между proto2 и proto3?
  A6: В proto3 убраны required, default, extensions; добавлена стандартная
      JSON-сериализация; изменена работа с enum.

  Q7: Как использовать Timestamp в Go?
  A7: Импортировать "google.golang.org/protobuf/types/known/timestamppb".
      Использовать timestamppb.New(time.Now()) и AsTime().

  Q8: Что такое Any и когда использовать?
  A8: Any — контейнер для любого protobuf-сообщения. Использовать, когда
      тип сообщения неизвестен заранее (event-системы).

  Q9: Как вернуть ошибку в gRPC с деталями?
  A9: Использовать status.New(...).WithDetails(...).

  Q10: Как проверить, что поле установлено в proto3?
  A10: С помощью optional и проверкой на nil в Go.
*/
