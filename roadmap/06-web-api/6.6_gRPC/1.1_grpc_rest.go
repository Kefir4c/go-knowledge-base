package grpc

/*
  РАЗДЕЛ 1.1: Что такое gRPC и зачем он нужен

   1.  ЗАЧЕМ ВООБЩЕ НУЖЕН gRPC? (ПРОБЛЕМА)

  До gRPC для общения между микросервисами чаще всего использовали REST+JSON.
  Это работало, но с ростом нагрузки и количества сервисов стали видны проблемы:

    • JSON — текстовый формат, он медленно парсится и много весит.
    • HTTP/1.1 не умеет мультиплексировать запросы по одному соединению.
    • Нет строгого контракта — документация часто расходится с реальностью.
    • Стриминг (передача данных в реальном времени) требует WebSockets.

  Google внутри себя десятилетиями использовал RPC-систему Stubby, которая
  решала эти проблемы. В 2015 году они открыли её как gRPC.

  2.  ИЗ ЧЕГО СОСТОИТ gRPC? (ТРИ КИТА)

  gRPC — это не просто библиотека, а целая экосистема.

  КИТ №1: PROTOCOL BUFFERS (protobuf) — язык описания данных и сервисов.
    • Ты пишешь .proto-файл, в котором описываешь структуры (message)
      и методы (service).
    • Из этого файла генерируется код на нужном языке (Go, Java, Python...).
    • Protobuf — бинарный формат сериализации. Он очень компактный и быстрый.
    • Это даёт строгий контракт: клиент и сервер всегда знают, какие поля есть.

  КИТ №2: HTTP/2 — транспорт.
    • gRPC работает только поверх HTTP/2.
    • HTTP/2 даёт мультиплексирование — много запросов по одному TCP-соединению.
    • Сжатие заголовков, server push, стриминг.

  КИТ №3: ГЕНЕРАЦИЯ КОДА.
    • По .proto-файлу генерируются клиентские и серверные заглушки (stubs).
    • Разработчик пишет только бизнес-логику, а всю сетевую часть берёт
      на себя сгенерированный код.

  3.  ТАК В ЧЁМ ЖЕ ОТЛИЧИЕ ОТ REST? (СРАВНЕНИЕ)

  Проще всего понять gRPC через сравнение с REST/JSON:

  ┌─────────────────────┬──────────────────────┬──────────────────────────┐
  │ Характеристика      │ gRPC                 │ REST/JSON                │
  ├─────────────────────┼──────────────────────┼──────────────────────────┤
  │ Транспорт           │ HTTP/2               │ HTTP/1.1 или HTTP/2      │
  ├─────────────────────┼──────────────────────┼──────────────────────────┤
  │ Формат данных       │ Бинарный (Protobuf)  │ Текстовый (JSON)         │
  ├─────────────────────┼──────────────────────┼──────────────────────────┤
  │ Размер сообщения    │ Меньше (в 3-10 раз)  │ Больше                   │
  ├─────────────────────┼──────────────────────┼──────────────────────────┤
  │ Скорость            │ Очень высокая        │ Средняя                  │
  ├─────────────────────┼──────────────────────┼──────────────────────────┤
  │ Стриминг            │ Встроенный           │ Требует WebSockets/SSE   │
  ├─────────────────────┼──────────────────────┼──────────────────────────┤
  │ Контракт (схема)    │ Обязательный (.proto)│ Опциональный (OpenAPI)   │
  ├─────────────────────┼──────────────────────┼──────────────────────────┤
  │ Человекочитаемость  │ Нет (бинарный)       │ Да (текстовый)           │
  ├─────────────────────┼──────────────────────┼──────────────────────────┤
  │ Генерация клиентов  │ Автоматическая       │ Ручная или через OpenAPI │
  ├─────────────────────┼──────────────────────┼──────────────────────────┤
  │ Мультиплексирование │ Да (HTTP/2)          │ Нет (HTTP/1.1)           │
  └─────────────────────┴──────────────────────┴──────────────────────────┘

  Главный вывод: gRPC — это про производительность и строгость, REST — про
  простоту и человекочитаемость.

  3.1 ТИП ДАННЫХ

  ┌─────────────────┬─────────────┬───────────────────────────────────────┐
  │ .proto Тип      │ Go Тип      │ Описание                              │
  ├─────────────────┼─────────────┼───────────────────────────────────────┤
  │ double          │ float64     │ 64-битное число с плавающей точкой    │
  ├─────────────────┼─────────────┼───────────────────────────────────────┤
  │ float           │ float32     │ 32-битное число с плавающей точкой    │
  ├─────────────────┼─────────────┼───────────────────────────────────────┤
  │ int32           │ int32       │ 32-битное целое                       │
  ├─────────────────┼─────────────┼───────────────────────────────────────┤
  │ int64           │ int64       │ 64-битное целое                       │
  ├─────────────────┼─────────────┼───────────────────────────────────────┤
  │ sint32          │ int32       │ 32-битное целое со знаком             │
  │                 │             │ (эффективнее для отрицательных чисел) │
  ├─────────────────┼─────────────┼───────────────────────────────────────┤
  │ sint64          │ int64       │ 64-битное целое со знаком             │
  ├─────────────────┼─────────────┼───────────────────────────────────────┤
  │ uint32          │ uint32      │ 32-битное беззнаковое целое           │
  ├─────────────────┼─────────────┼───────────────────────────────────────┤
  │ uint64          │ uint64      │ 64-битное беззнаковое целое           │
  ├─────────────────┼─────────────┼───────────────────────────────────────┤
  │ bool            │ bool        │ Логический тип                        │
  ├─────────────────┼─────────────┼───────────────────────────────────────┤
  │ string          │ string      │ Строка UTF-8                          │
  ├─────────────────┼─────────────┼───────────────────────────────────────┤
  │ bytes           │ []byte      │ Произвольная последовательность байт  │
  └─────────────────┴─────────────┴───────────────────────────────────────┘

  4.  КАК ВЫГЛЯДИТ .PROTO-ФАЙЛ? (КОНТРАКТ)

  Это сердце gRPC. Вот пример:

    syntax = "proto3";                      // версия синтаксиса
    package user.v1;                       // пространство имён
    option go_package = ".;userpb";        // куда генерировать Go-код

    // Сообщение — аналог struct в Go
    message User {
      int32  id = 1;        // поля 1-15 кодируются в 1 байт
      string name = 2;
      string email = 3;
      repeated string tags = 4;  // слайс
    }

    // Сервис — набор RPC-методов
    service UserService {
      rpc GetUser (GetUserRequest) returns (User) {}
      rpc ListUsers (ListUsersRequest) returns (stream User) {}
    }

    message GetUserRequest { int32 id = 1; }
    message ListUsersRequest { int32 limit = 1; }

  Из этого файла с помощью protoc генерируются два файла:
    • user.pb.go — структуры сообщений
    • user_grpc.pb.go — интерфейсы сервера и клиента

  5.  КАКИЕ БЫВАЮТ ТИПЫ RPC? (4 ВИДА)

  В зависимости от того, сколько запросов и ответов, gRPC поддерживает
  4 типа взаимодействия. Это одно из главных отличий от REST.

  1. Unary RPC (один запрос → один ответ)
     Самый простой, как обычный HTTP-запрос. Используется для CRUD.

  2. Server-Streaming (один запрос → поток ответов)
     Клиент отправляет один запрос, а сервер возвращает несколько сообщений
     по одному. Полезно для списков, подписок, выгрузки больших данных.

  3. Client-Streaming (поток запросов → один ответ)
     Клиент отправляет несколько сообщений, сервер собирает их и возвращает
     один ответ. Для загрузки файлов, пакетной обработки.

  4. Bidirectional Streaming (поток запросов ↔ поток ответов)
     Обе стороны отправляют сообщения независимо. Для чатов, игр,
     интерактивных систем.

  ┌─────────────────────┬─────────────┬─────────────┬─────────────────────┐
  │ Тип                 │ Запросов    │ Ответов     │ Когда использовать  │
  ├─────────────────────┼─────────────┼─────────────┼─────────────────────┤
  │ Unary               │ 1           │ 1           │ CRUD, запрос-ответ  │
  ├─────────────────────┼─────────────┼─────────────┼─────────────────────┤
  │ Server-Streaming    │ 1           │ N           │ Списки, подписки    │
  ├─────────────────────┼─────────────┼─────────────┼─────────────────────┤
  │ Client-Streaming    │ N           │ 1           │ Загрузка, пакетная  │
  ├─────────────────────┼─────────────┼─────────────┼─────────────────────┤
  │ Bidirectional       │ N           │ N           │ Чаты, интерактив    │
  └─────────────────────┴─────────────┴─────────────┴─────────────────────┘

  6.  КАК ПЕРЕДАВАТЬ ДОПОЛНИТЕЛЬНУЮ ИНФОРМАЦИЮ? (METADATA)

  В HTTP есть заголовки (headers). В gRPC их роль играет metadata.

  Это key-value пары, которые передаются вместе с запросом или ответом.
  Чаще всего через них передают:
    • JWT-токены (authorization: Bearer ...)
    • trace_id для распределённой трассировки
    • request_id для связывания логов

  Как это выглядит в коде:

    // Клиент отправляет metadata
    md := metadata.Pairs("authorization", "Bearer my-jwt-token")
    ctx := metadata.NewOutgoingContext(context.Background(), md)
    resp, err := client.GetUser(ctx, req)

    // Сервер читает metadata (обычно в интерсепторе)
    md, ok := metadata.FromIncomingContext(ctx)
    token := md.Get("authorization")[0]

  7.  КАК ДОБАВЛЯТЬ СКВОЗНУЮ ЛОГИКУ? (INTERCEPTORS)

  Interceptors — это middleware для gRPC. Они позволяют выполнять код
  до и после обработки запроса, не меняя бизнес-логику.

  Типичные задачи:
    • Логирование запросов и ответов
    • Аутентификация (проверка JWT из metadata)
    • Сбор метрик (время ответа, количество запросов)
    • Восстановление после паник (recovery)
    • Rate limiting

  Пример интерсептора для проверки JWT:

    func AuthInterceptor() grpc.UnaryServerInterceptor {
        return func(ctx context.Context, req interface{},
            info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (interface{}, error) {

            md, _ := metadata.FromIncomingContext(ctx)
            auth := md.Get("authorization")
            if len(auth) == 0 || auth[0] != "Bearer secret" {
                return nil, status.Error(codes.Unauthenticated, "invalid token")
            }
            // Кладём userID в контекст для обработчиков
            ctx = context.WithValue(ctx, "userID", "123")
            return handler(ctx, req)
        }
    }

  Применяется при создании сервера:

    s := grpc.NewServer(grpc.UnaryInterceptor(AuthInterceptor()))

  Можно строить цепочки интерсепторов (с помощью go-grpc-middleware),
  чтобы применять несколько слоёв: recovery → logging → auth.

  8.  КАК НЕ ДОПУСТИТЬ КАСКАДНОГО ОТКАЗА? (DEADLINES)

  Это самый важный механизм надёжности в gRPC.

  Если клиент не установит таймаут, а сервер зависнет, весь запрос
  повиснет навсегда. В микросервисной архитектуре это приводит к
  каскадному отказу — один медленный сервис тянет за собой все остальные.

  Решение: deadlines (сроки). Клиент устанавливает дедлайн, он передаётся
  через контекст в цепочку вызовов. Каждый сервис знает, сколько времени
  осталось.

  На клиенте:
    ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
    defer cancel()
    resp, err := client.GetUser(ctx, req)

  На сервере проверяем:
    if ctx.Err() == context.DeadlineExceeded {
        return nil, status.Error(codes.DeadlineExceeded, "deadline exceeded")
    }

  В долгих циклах проверяем периодически:
    for _, item := range items {
        select {
        case <-ctx.Done():
            return nil, status.Error(codes.DeadlineExceeded, "deadline exceeded")
        default:
            // продолжаем
        }
    }

  9.  КАК ПРАВИЛЬНО ОБРАБАТЫВАТЬ ОШИБКИ? (STATUS CODES)

  В gRPC ошибки возвращаются через механизм статусов с кодами из пакета codes.
  Это делает обработку ошибок предсказуемой на всех языках.

  Основные коды (запомни эти!):

    • OK (0) — успех
    • NotFound (5) — ресурс не найден (аналог 404)
    • InvalidArgument (3) — неверные данные (400)
    • Unauthenticated (16) — не авторизован (401)
    • PermissionDenied (7) — нет прав (403)
    • DeadlineExceeded (4) — таймаут (504)
    • Internal (13) — внутренняя ошибка (500)
    • Unavailable (14) — сервис недоступен (503)
    • Unimplemented (12) — метод не реализован (501)

  Сервер возвращает ошибку:
    return nil, status.Error(codes.NotFound, "user not found")

  Клиент обрабатывает:
    st, ok := status.FromError(err)
    if ok {
        switch st.Code() {
        case codes.NotFound:
            // пользователь не найден
        case codes.DeadlineExceeded:
            // таймаут
        }
    }

  10. КОГДА ИСПОЛЬЗОВАТЬ gRPC, А КОГДА — REST? (ВЫВОД)

  gRPC — идеальный выбор для:
    • Внутренних микросервисов (высокая производительность, строгие контракты)
    • Систем с высокой нагрузкой
    • Стриминга и real-time
    • Когда нужны клиенты на разных языках (генерируется код)
    • Mobile-приложений (экономия трафика и батареи)

  gRPC используют в продакшене: Google, Netflix, Square, Docker, CoreOS.

  REST всё ещё лучше для:
    • Публичных API (человекочитаемость, браузеры)
    • Простых CRUD-приложений
    • Интеграции со сторонними системами
    • Когда скорость не критична, а простота важнее


  11. ИНСТРУМЕНТЫ И ЭКОСИСТЕМА

  • grpcurl — аналог curl для gRPC
  • grpc-gateway — REST-прокси из .proto
  • buf — линтинг, форматирование, генерация
  • grpc-web — поддержка в браузерах
  • Reflection — позволяет Postman/grpcurl видеть методы без .proto
  • go-grpc-middleware — готовые интерсепторы (logging, auth, recovery)

  gRPC — проект CNCF, индустриальный стандарт.

  12. ПРОИЗВОДИТЕЛЬНОСТЬ (ЦИФРЫ)

  • Задержка: gRPC 15-20 мс vs REST 120-150 мс
  • Пропускная способность: gRPC 7327 req/s vs REST 930 req/s
  • Сериализация: Protobuf в 6 раз быстрее JSON, сообщения на 30-50% меньше
  • При высокой нагрузке gRPC стабилен, REST-TLS падает до 1.1% успешности

  13. КЛЮЧЕВЫЕ ВЫВОДЫ ДЛЯ СОБЕСЕДОВАНИЯ

  1. gRPC — RPC-фреймворк Google на HTTP/2 с бинарным protobuf.
  2. В 5-10 раз быстрее REST, сообщения меньше в 3-10 раз.
  3. 4 типа RPC: Unary, Server-Streaming, Client-Streaming, Bidirectional.
  4. Interceptors — middleware для gRPC (логирование, auth, метрики).
  5. Deadlines — таймауты, пропагируемые через цепочку, без них — каскадный отказ.
  6. Ошибки через status.Error и коды из codes.
  7. Metadata — заголовки для JWT, trace_id, request_id.
  8. Использовать: микросервисы, high-load, real-time, mobile.
  9. Не использовать: публичные API, простые CRUD, браузеры.
  10. gRPC — проект CNCF, используется в Google, Netflix, Square.
*/
